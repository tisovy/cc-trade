schema: spec-driven
created: 2026-08-09
# This change verifies existing requirements against a live account. It adds,
# modifies and removes nothing, so it carries no spec delta by design.
skip_specs: true
