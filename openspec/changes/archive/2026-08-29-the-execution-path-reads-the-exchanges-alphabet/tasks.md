# Tasks

## 0. Measured first, 2026-08-28/29

- Слово оператора: скрин гейта LISTING на 龙虾USDT + «торговля заблокирована …
  именно на этой паре» (23 ордера, 2 позиции на счёте).
- Отказ живёт в одном выражении: `tradable:
  ASCII_EXECUTION_SYMBOL_PATTERN.test(...)`
  (`futures-workstation-market-contract.js:72,345`). Валидация команд алфавит
  не сужает (только «непустой»); подпись строится по той же percent-encoded
  строке, что уходит на провод; маркет- и history-чтения с этим тикером уже
  доказаны живьём. clientOrderId из символа не строится нигде.

## 1. Implementation

- [x] 1.1 `tradable` = каталогизирован ∧ `TRADING` ∧ `PERPETUAL`;
      `ASCII_EXECUTION_SYMBOL_PATTERN` удалён.
- [x] 1.2 Тест «…without execution authority» перевёрнут: юникодный
      TRADING PERPETUAL теперь `tradable: true`; статус/тип-условия пинятся
      прежними тестами (PENDING_TRADING → false, CURRENT_QUARTER → false).
- [x] 1.3 Гейт LISTING и его derivation в тикете не тронуты (страж
      расхождения на будущее); их юнит-тесты остаются.
- [x] 1.4 Bite-проверка новых/перевёрнутых ассертов против кода до правки
      (git archive → scratch-прогон), полный прогон, линт, стражи.

## 2. Operator verification (runbook, live)

- [x] 2.1 Открыть 龙虾USDT: кнопки LONG/SHORT entry/exit активны, гейта
      LISTING нет.
- [x] 2.2 Вкладка истории по паре наполняется (подписанные запросы с
      CJK-символом доказаны).
- [x] 2.3 Канарейка: лимитный ордер минимального размера далеко от рынка →
      виден в Orders (и в бинанс-аппе) → отменить с деска. Ошибок -1100/-1121
      в терминале нет.
- [x] 2.4 Рабочий ордер: поставить/перетащить/отменить обычным жестом.

Закрыто 2026-08-29 журналом desk-2026-08-29-000: канарейка исполнена live —
`trade.placeOrder` 07:38:36Z → `trade.cancelOrder` 07:39:04Z, затем ещё
7 ордеров и cancel обычными жестами, все 71 ответ за день `ok`, ошибок
-1100/-1121 нет. Слово оператора: «всё работает нормально».
