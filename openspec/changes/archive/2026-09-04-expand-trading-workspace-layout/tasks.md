## 1. Responsive Page Shell

- [x] 1.1 Remove the Futures workstation's fixed desktop width ceiling, preserve the responsive edge inset, and constrain page overflow by axis; verify the production stylesheet gives the direct workstation child the full padded content width with no page-level horizontal scrolling.

## 2. Presentation Coverage

- [x] 2.1 After the production layout is in place, add regression coverage for full-width growth, viewport containment, and preserved vertical scrolling; verify the focused Futures workstation presentation tests pass.

## 3. Integrated Verification

- [x] 3.1 Validate the OpenSpec change and run the relevant lint, build, and browser-sized layout checks; verify wide and constrained viewport measurements show no horizontal overflow and no content outside the page edge.

## 4. Contract Identity Allocation

- [x] 4.1 Expand the market-header contract column in proportion to the existing interface scale and contain exceptional long identities; verify `USELESSUSDT` fits before the divider at 105% without reducing the adjacent readings to overflow.

## 5. Contract Identity Coverage

- [x] 5.1 After the production sizing is in place, add regression coverage for the scale-aware allocation and long-name containment; verify the focused Futures workstation presentation tests pass.

## 6. Final Audit

- [x] 6.1 Revalidate OpenSpec and run browser measurements, full tests, lint, build, diff checks, and GitNexus change detection; fix any in-scope regression before marking the change ready to sync and archive.
