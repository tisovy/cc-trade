# Операторский runbook — 2026-08-25

Все оставшиеся живые проверки активных изменений, собранные в один прогон.
Ревизия деска: `9758784`. Журнал: `~/.config/cc-trade/diagnostics/`.
Прокси: `127.0.0.1:1080`.

**Статус прогона 2026-08-25** (детали — в ledger, «The 2026-08-25 Operator
Runbook Pass»): A1 ✓ с находкой (заголовок аппа = наш НЕТ; остался вопрос
метки и экрана), A3 ✓ (правка `c8da790`: 2 знака), B1 ✓, B2 ✓ (BNB
тратится; income-строка — за пробой), D1 ✓, D2 ✓ (журнал: SOCKET_CLOSED →
ECONNREFUSED, ок в 07:26:21Z). B3 ✓ (2026-08-25: «Wallet Net», обе
величины в title, апп до цента — оба BNB-гейта закрыты). B4 ✓ (2026-08-25,
`14fb863`: открыл позицию — «ошибки не было»; журнал подтверждает, что случай
возник: филл 18:27:20.426Z поставил долг, подтверждающий проход 18:29:23.336Z
его снял сам). **Открыты: A2, C1** и имя экрана аппа (вопрос 1.4 в
`close-a-round…`).

Ревизия для оставшихся шагов: **`14fb863`**.

Как записывать: для каждого шага — время (чч:мм:сс), символ, точные цифры
(из title, не с глаза), и вердикт «совпало / не совпало / не случилось».
Скриншоты приветствуются. Результаты отдать сессии Клода — она разнесёт по
`live-verification-ledger.md` и закроет таски.

---

## Блок A — только посмотреть, ничего не трогая

### A1. Четыре закрытые строки против бинанс-аппа (главная сверка)

Закрывает: `close-a-round-at-what-reached-the-wallet` 4.5 + 6c.3 + 1.4,
`read-the-settled-money-from-the-newest-end` 5.2 + 9.1,
`verify-final-futures-pnl-live-data` 1.1.

1. В деске: History → вкладка закрытых позиций. Взять **четыре** строки:
   лучше те, что расходились 2026-08-24 (среди них CYSUSDT, BTWUSDT,
   BEATUSDT), плюс любая свежая. Если будет частичное закрытие или разворот —
   включить их.
2. Для каждой строки навести мышь на число в колонке PnL и **выписать из
   всплывающей подсказки целиком**: точное значение (`Exact …`, если есть),
   строку `Wallet Net: …` или `Visible net: …`, и разложение
   (realized / commission / funding).
3. В бинанс-аппе открыть **Position History** (именно этот экран — записать
   его название, это отдельный открытый вопрос 1.4) и найти те же позиции.
   Выписать «Реализ. PnL» каждой.
4. Сравнить: **колонка деска = «Реализ. PnL» аппа с точностью до цента.**
5. Если по строке есть разница ~единицы USDT — посмотреть в тултипе
   компоненту funding. Ожидание из пробы 2026-08-24: разницу объясняет
   funding, который апп кладёт в строку, а деск честно держит в «shared
   adjustments» (CYSUSDT +2.375, BTWUSDT +16.55, BEATUSDT −413.36). Если
   тултипный funding закрывает разницу — зачёт. Если нет — выписать все три
   числа разложения по расходящейся строке.

Записать: 4 × (символ, время закрытия, цифра деска, exact из title,
цифра аппа, вердикт) + название экрана аппа.

### A2. Счёт закрытых позиций против аппа

Закрывает: `show-one-pnl-and-let-the-operator-size-the-dock` 3.4.

1. В деске посчитать, сколько строк в Closed Positions (окно деска — ровно
   7 дней).
2. В аппе отлистать Position History назад до даты «сегодня минус 7 дней» и
   посчитать закрытия **за тот же период**.
3. Числа равны → зачёт. Если в аппе больше — выписать символ и время
   закрытия каждой позиции, которая есть в аппе и нет в деске. Вопрос,
   который решается: все ли «лишние» старше 7-дневного окна.

### A3. Резерв BNB на глаз

Закрывает: живую половину архивного `value-the-bnb-commission-in-the-result`
(readout до первого филла — единственное наблюдаемое).

1. В шапке дока найти «BNB fee reserve»: должно быть «1 BNB ≈… USDT»
   (количество и стоимость, при стоимости ниже 50 USDT — пометка low).
2. Сравнить с балансом BNB в аппе (кошелёк USDⓈ-M): количество совпадает,
   стоимость ≈ количество × курс BNB.

---

## Блок B — обычная торговля (одна сессия)

### B1. Закрытие по market с первого клика

Закрывает: `close-the-position-the-desk-is-showing` 5.1.

1. В обычной торговле закрыть любую позицию кнопкой Close (market)
   **один раз** и засечь.
2. Зачёт: ушла с первого клика. Если отказ — записать секунду-в-секунду
   время и текст попапа: он теперь обязан назвать условие (NO_READING /
   STALE_READING / QUANTITY_EXCEEDS_LEG / LEG_MISMATCH / SIDE_MISMATCH).

### B2. Первый BNB-филл: три факта с провода

Закрывает: `value-the-bnb-commission-in-the-result` 1.2 (ledger,
OUTSTANDING).

1. Сделать любую маленькую сделку (первую после включения BNB-скидки) и
   записать символ и время до секунды.
2. Сообщить сессии Клода символ+время — она снимет с провода: (а)
   `commissionAsset` и сумму на филле из `userTrades`, (б) asset
   income-строки COMMISSION (одноразовый подписанный запрос, деск эту
   линию не читает).
3. Третий факт — за вами: в аппе Position History по этой сделке — **входит
   ли комиссия в «Реализ. PnL»** (сравнить число аппа с грубой прикидкой
   (выход−вход)×размер, или скрин). Записать «входит / не входит».

### B3. Первый закрытый BNB-раунд против аппа

Закрывает: `value-the-bnb-commission-in-the-result` 5.1 (ledger,
OUTSTANDING).

1. После закрытия сделки из B2: в деске колонка PnL по этой строке =
   «Реализ. PnL» аппа с точностью до цента (класс округления в один цент —
   как 0.37/0.38 на VELVETUSDT — не дефект).
2. Навести на число: в title должно быть «fee X BNB valued −Y USDT at
   BNBUSDT Z (…)» — количество BNB, оценка в USDT и использованная цена.
   Если вместо этого «fee X BNB not included — no readable …» — записать
   как есть (это деградация, не падение, но её надо зафиксировать).
3. Взглянуть на «BNB fee reserve»: количество уменьшилось на комиссию.
4. На фейсе строки не должно быть **никакой** второй строки с BNB — только
   одно USDT-число.

### B4. Ложный попап «refresh failed» снят — ✓ пройдено 2026-08-25

Закрывало: `say-the-announced-charge-is-still-posting` 5.1 (закрыт, изменение
заархивировано). Шаги оставлены как образец на случай рецидива.

Что было: при открытии и закрытии позиции в течение двух минут выскакивал
попап «Wallet-adjustment refresh failed. Closed-position PnL keeps the
confirmed reading from …. Press ↻ to retry.» Ничего не падало: биржа
объявляет заряд по сокету, а строку в income пишет позже, и деск называл это
ожидание провалом. В `efa9d4a` ожидание отделено от провала.

1. Открыть позицию, затем закрыть её (годится та же сделка, что в B1/B2).
2. **Две минуты после каждого действия** смотреть на попапы. Зачёт: попапа
   «Wallet-adjustment refresh failed» нет ни разу. Если выскочил — записать
   время посекундно и полный текст.
3. Навести курсор на число в строке этой закрытой позиции. В первые ~2 минуты
   в title должно стоять: **«A charge the exchange announced is still
   posting; confirming at ЧЧ:ММ:СС»** — вместо прежнего общего «Settled-income
   verification is not ready». Записать, стояло ли, и названное время.
4. Подождать до этого названного времени, ничего не нажимая. Зачёт: строка
   сама доходит до метки **Wallet Net** и цифры встают — без единого ↻.
5. Контроль (можно совместить с D1): при **настоящем** сбое — прокси
   остановлен — попап «refresh failed» обязан выскочить как раньше, один раз,
   с отметкой удержанного чтения и с ↻.

---

## Блок C — funding-граница (позиция через 03:00 / 11:00 / 19:00 МСК)

### C1. Funding появляется в строке открытой позиции

Закрывает: `state-what-an-open-position-has-already-paid` 5.8,
`close-a-round-at-what-reached-the-wallet` 4.6,
`verify-final-futures-pnl-live-data` 1.3.

1. Держать позицию открытой через границу фандинга (у большинства
   контрактов 03:00/11:00/19:00 МСК; точный момент — Countdown в аппе).
2. В первые ~2 минуты после границы смотреть на PnL-ячейку строки: в
   тултипе должна появиться компонента funding. Записать, через сколько
   секунд/минут появилась.
3. Попап «Wallet-adjustment refresh failed… press ↻» в эти же 2 минуты
   больше выскакивать не должен (снят в `efa9d4a` — см. B4). Если всё же
   выскочил — записать время посекундно, это провал B4, но не C1.
4. Если позиция потом закроется — сверить строку в Closed с аппом: funding
   в разложении на месте.

---

## Блок D — прокси-блок (в спокойный момент, без висящих ордеров!)

### D1. Сбой settled-чтения → восстановление без рестарта

Закрывает: `verify-final-futures-pnl-live-data` 1.4 (вторая половина;
рестарт-половина подтверждена 2026-08-24).

1. Записать время. Остановить прокси `127.0.0.1:1080`.
2. В деске нажать ↻: должно появиться честное сообщение о сбое с меткой
   последнего подтверждённого чтения; цифры на экране остаются старыми, а
   не пустыми. Записать текст и метку.
3. Запустить прокси обратно, нажать ↻: значения обновились, сбой ушёл.
   Записать время восстановления.

### D2. Стрим называет свой обрыв

Закрывает: `prove-the-private-stream-is-carrying` 5.3 (шаг 50 старого
runbook'а).

1. Ничего дополнительно делать не надо — это та же процедура D1. От вас
   только: точное время остановки и запуска прокси.
2. Дальше сессия Клода читает журнал: при обрыве должны появиться строки
   `futures-user-data` с `SOCKET_CLOSED`, затем `RECONNECT_EXHAUSTED`;
   в обычной **тихой** сессии таких строк быть не должно вовсе (назовите
   любое спокойное окно часа — проверим и это).
3. Опционально (можно пропустить, шаг это допускает): вместо остановки —
   заморозить прокси `kill -STOP <pid>`, подождать ~7 минут, `kill -CONT`.
   Это единственный способ получить «молчащий сокет», а не обрыв.

---

## Когда случится само (не искать специально)

- **Hedge LONG+SHORT на одном контракте** (`verify-final` 1.2, вторая
  половина): если когда-нибудь будут обе ноги разом — у каждой строки свой
  PnL, не общий на контракт.
- **Повторный отказ close** (B1): если случится — попап обязан назвать
  условие.

## Что от оператора НЕ требуется

- `say-the-announced-charge-is-still-posting` 5.1 — только после
  имплементации (код не написан, попап пока ложно срабатывает — блок C1 п.3).
- `verify-final` 2.1 (проба) — уже прогнана 2026-08-24; повтор по желанию:
  `BFK=… BFS=… node scripts/probe-futures-settled.mjs`.
- `verify-final` 2.2–2.4 (веса/латентность/429, замер персиста) и 3.1–3.2
  (разнос по ledger) — журнальная работа, сделает сессия Клода по итогам
  ваших прогонов.
- `verify-final` 1.5, `read-the-settled…` 9.3, `show-one-pnl…` 2a.4 —
  закрываются записью, действий не требуют.
