# Tasks

## 0. Measured first, 2026-08-28 evening (journal -002, after the CJK deploy)

- Полные ресинки сессии `MALFORMED_STREAM_FRAME` на показанной 龙虾USDT:
  19:51:16, 19:51:33, 19:52:33, 19:53:26, 19:53:54, 19:53:58 — каждые 20–60 с,
  цикл `resynchronizing` → `loading` (~0.5 с) → `live` (~1.6–2 с). Оператор:
  «график дергается туда-сюда».
- Перед каждым — `fault stream CROSSED_ORDER_BOOK symbol 龙虾USDT` (бросок из
  `applyDelta`, книга уже пометила себя `RESYNC_REQUIRED`) и рядом
  `DEPTH_BAND_WALKED` (~каждые 11 с, пара пампит).
- Классификатор `DEPTH_STREAM_NAME` = `[a-z0-9_]{1,32}@depth`; имя стрима на
  проводе — сырой юникод `龙虾usdt@depth@100ms` (доказано доставкой кадров:
  нормализатор сверяет `envelope.stream` с `symbol.toLowerCase()`-набором).
  Любая форма CJK-имени (raw, percent, `\u`) мимо класса → книжный бросок
  эскалирует в ресинк сессии.
- Смена пары при этом уже НЕ происходит (display-строки держат 龙虾USDT
  непрерывно с 19:37:35) — дёргание целиком за счёт ресинков.

## 1. Implementation

- [x] 1.1 Catch `handleStreamFrame` маршрутизирует по ошибке прежде кадра:
      `FuturesWorkstationOrderBookError` → `recoverBook(session,
      safeCode(error))` — книга, не сессия; код причины — собственный код
      броска (`CROSSED_ORDER_BOOK`), не обобщённый `MALFORMED_DEPTH_FRAME`.
- [x] 1.2 `DEPTH_STREAM_NAME` читает орфографию биржи:
      `(?:[^"\\]|\\u[0-9a-fA-F]{4}){1,96}@depth` — сырой юникод и `\u`-эскейпы,
      ограничено кавычкой имени.
- [x] 1.3 Не-depth мусор по-прежнему ресинкает сессию — существующий тест
      «treats malformed stream data as uncertain…» не тронут и зелёный.
- [x] 1.4 Тесты кусались: три новых теста (живая сессия 龙虾USDT на
      переименованной фикстуре — точная форма провода; скрещивающая дельта в
      секвенции; нечитаемый depth-кадр; ASCII-кросс) — все три красные на
      коде до правки, зелёные после. Полный прогон: 128 файлов / 2959 тестов,
      линт, 4 стража.

## 2. Operator verification (runbook)

После мержа (деск перезапустится сам), на живом 龙虾USDT:

- [ ] 2.1 `grep '"state":"resynchronizing"' по свежему журналу: строк с
      `MALFORMED_STREAM_FRAME` больше не прибавляется (до фикса — каждые
      20–60 с).
- [ ] 2.2 На их месте — `fault book-recovery CROSSED_ORDER_BOOK symbol
      龙虾USDT`: книга пересобирается тихо, статус пары остаётся `live`,
      график не перегружается.
- [ ] 2.3 Глазами: дёргание прекратилось; допустимый остаток — редкое
      короткое обновление стакана (band re-centre), без ухода в loading.
