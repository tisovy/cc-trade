## 1. Change Safety

- [x] 1.1 Run GitNexus upstream impact analysis for every React function/component whose implementation will change and report the blast radius before editing production code

## 2. Production Implementation

- [x] 2.1 Thread the existing self-updating market clock into the mounted Futures workstation while preserving the app-level clock during Spot and activation loading
- [x] 2.2 Recompose the Futures grid and mode-scoped CSS so the blue identity strip begins at the top, the switch overlays it, and the clock owns a dedicated non-overlapping row with responsive control spacing

## 3. Verification Coverage

- [x] 3.1 Add focused renderer and style-contract tests for Futures clock ownership, header order, zero top inset, switch overlay, and responsive layout safeguards
- [x] 3.2 Update the shared Gateway boundary check to allow the new presentation-only clock prop while continuing to pin the existing transport props
- [x] 3.3 Run the focused App/Futures test suites and the repository UI quality checks, fixing any regression caused by the implementation

## 4. OpenSpec and Change Review

- [x] 4.1 Revalidate the OpenSpec change and review the completed checklist for exact alignment with the proposal and delta spec
- [x] 4.2 Run GitNexus change detection against `main`, confirm only the expected symbols and flows changed, and commit the completed change directly to `main`
