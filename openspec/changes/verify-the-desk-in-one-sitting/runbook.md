# Прогон проверки деска — один заход

> This runbook is written in Russian on purpose: the operator is its only
> executor, and a checklist you have to translate while running it is a
> checklist that gets skipped. The proposal and the task list stay in English.

Сорок шесть шагов, около двух с половиной часов, подряд. Прочитайте разделы «Во
что обойдётся» и «Порядок и почему он такой» до начала — дальше сюрпризов не
будет.

Записан результат девяти шагов; у тридцати семи записи нет. Четыре из девяти
записаны 12–13 августа, до того как переделали стакан — шаги 2 и 3 стоит
переспросить, остальные к книге не относятся.

---

## Для сессии, которая ведёт прогон

Вы ведёте, оператор выполняет. Правила:

1. **Один шаг за раз.** Дайте текст шага целиком — «что сделать» и «что должно
   быть» — и остановитесь. Не выдавайте следующий, пока не получили ответ.
2. **Не объединяйте шаги.** Даже соседние и даже похожие. Оператор ведёт три
   сессии сразу; пакет из четырёх шагов гарантированно превратится в один
   невнимательный ответ.
3. **Записывайте дословно.** Если оператор написал «висит секунд пять, потом
   рисует» — так и запишите. Не превращайте в «PASS с задержкой».
4. **На `FAIL` — остановитесь.** Спросите ещё два-три уточняющих факта (что в
   терминале, что на экране, что на Binance), запишите и **не продолжайте
   прогон**, если шаг ставит под сомнение следующие. Явно скажите оператору, что
   останавливаетесь и почему.
5. **Перед Частью 3 предупредите отдельно.** Это реальные ордера. Спросите
   подтверждение, что момент подходящий, и назовите цену из раздела «Во что
   обойдётся».
6. **Шаги 20 и 21 не торопите.** Шаг 20 требует, чтобы деск не трогали две
   минуты подряд; два прошлых захода были испорчены именно тем, что его
   торопили.
7. **В конце соберите таблицу** из раздела «Результаты» и сохраните её. Только
   после этого можно проставлять галочки в `tasks.md` — и только те, которые
   запись подтверждает.
8. **Шаг может закрывать задачу уже заархивированного изменения.** Так устроено
   больше десятка шагов: изменение архивируется с переданной сюда операторской
   задачей, оставленной незакрытой, — передача даёт право на архив, галочку
   ставит только запись оператора. Ссылка в строке «Закрывает» остаётся по имени
   изменения, без даты архива: имя каталога в `openspec/changes/archive/`
   начинается с даты, но искать проще по имени, и все прежние ссылки сделаны
   так. Саму запись кладите в ledger из `state-only-verified-completion`, а не в
   архивный `tasks.md`.
9. **Не подсказывайте ожидаемый ответ.** «Видите ли вы, что стакан заполнен?» —
   плохо. «Что показывает стакан?» — хорошо.

---

## Что нужно

- Деск, запущенный на живом Production-аккаунте (`npm run e`).
- `FUTURES_MAX_ORDER_USDT` выставлен — чуть выше минимального номинала
  контракта, которым будете торговать. Это самая дешёвая страховка на весь
  прогон, и шаг 27 без неё не проверяется.
- Место, куда писать по строке на шаг. Строка и есть смысл прогона: шаг, который
  никто не записал, не состоялся.
- Ликвидный контракт, которым вы готовы торговать минимальным размером. BTCUSDT —
  безопасный вариант по умолчанию: минимальный номинал маленький относительно
  ликвидности.
- Второй контракт — любой, для шага 23.
- Тонкий контракт, у которого стакан часто пересобирается (BEAT — та самая), и
  **самая неторгуемая крипто-монета**, какую найдёте, для шагов 7 и 10.
- Доступ к локальному прокси (`127.0.0.1:1080`). Его остановка — это то, чем
  прогон отрезает деск от Binance, ничего больше не трогая: внутренний сокет
  деска при этом жив, приложение работает и честно показывает, что биржи не
  видит.
- Приложение или сайт Binance под рукой: шаги 25, 29 и 32 сверяются с биржей, а
  не с деском.

## Во что обойдётся

**Время.** Часть 1 — 40–50 минут, Часть 2 — 20–25 минут (из них 2 минуты деск
просто стоит с отрезанной биржей и его нельзя трогать), Часть 3 — 70–85 минут.
Итого **около двух с половиной часов**. Часть 1 и Часть 2 можно пройти и остановиться:
Часть 3 не зависит от них ничем, кроме того, что деск к ней уже проверен.

**Ордера.** Части 1 и 2 не размещают ничего, кроме **одного** лимитного ордера
минимального размера далеко от рынка — он ставится перед обрывом, нужен шагу 18
и отменяется на шаге 21. Часть 3 размещает примерно **35 ордеров** минимального
размера — тридцать четыре плюс один на шаг 45; шаги 42, 43 и 44 не размещают
ничего. Подавляющее большинство — лимитные далеко от рынка, которые отменяются
внутри своего же шага.

**Сделки.** Исполниться должны **семь** ордеров: по одному в шагах 25 и 31, по
два в шагах 30 и 32, один порождённый в шаге 33. Всё остальное отменяется
неисполненным.

**Деньги.** Тейкерская комиссия Binance USDⓈ-M — 0,05% от номинала (0,04% с BNB).
При минимальном номинале около 100 USDT это ≈0,05 USDT на сделку, то есть
**порядка 0,3–0,5 USDT комиссии за весь прогон**.

**Чем вы рискуете сверх комиссии.** Одним: в шагах 30, 31 и 32 позиция
минимального размера стоит открытой от нескольких секунд до минуты, и рынок в
это время может пойти против вас. На номинале 100 USDT движение в 1% — это
1 USDT. Реалистичный потолок всего прогона — **несколько USDT**, а не десятки.

**Что стоит на бирже одновременно.** В пике — шаг 29 — два ордера минимального
размера плюс те, что остались с предыдущих шагов. Все далеко от рынка и
исполниться не должны. Если сомневаетесь — перед Частью 3 отмените всё, что у
вас уже висит, чтобы список на экране был ваш и только ваш.

**Один шаг рискует больше остальных.** Шаг 33 (сработавший ALGO) требует стопа,
который **должен сработать**, — то есть цена должна дойти до триггера. Это
единственный шаг, где вы не выбираете момент исполнения. Если не хотите его
делать — скажите, он последний и ничего за собой не тянет.

## Как записывать шаг

Для каждого шага — одно из:

- `PASS` — увидели то, что описано.
- `FAIL — <что увидели вместо>` — пишите фактическое наблюдение, не оценку.
- `NOT OBSERVABLE — <почему>` — ситуация не возникла.

## Порядок и почему он такой

Три части, и порядок между ними не произвольный:

1. **Часть 1** — всё, что читается при живой бирже и не стоит ни копейки.
   Сначала потому, что если деск здесь сломан, платить за Часть 3 незачем.
2. **Часть 2** — обрыв связи. **Один на весь прогон**, а не по разу на
   изменение: раньше каждое изменение просило свой обрыв, и прогон превращался в
   десять перезапусков. Все наблюдения при отрезанной бирже собраны здесь, в
   одно окно.
   Обрыва всё-таки два, и это минимум, а не небрежность: шаг 21 делается на
   **Spot**, а переключение на Spot пересобирает рыночную сессию и обнуляет
   счётчик попыток переподключения — то есть уничтожает то, что мерит шаг 20.
   Поэтому Spot получает свой отдельный короткий обрыв **после** шага 20, когда
   мерить уже нечего.
3. **Часть 3** — настоящие ордера. Последней, чтобы деньги тратились только на
   то, что уже проверено бесплатно.

**Шаги 42, 43, 44 и 46 не размещают ничего и ничего не стоят** — это чтение стакана,
и они лежат в Части 3 только потому, что были дописаны последними. Ведущая
сессия должна предложить их **сразу после Части 1**, а не в конце: они проверяют
то, что менялось позже всего, и если стакан сломан, это лучше узнать до того, как
оператор потратит час и деньги на Часть 3. Номера не переставлены намеренно — на
них ссылаются задачи заархивированных изменений, и переименование ссылок ради
порядка чтения обошлось бы дороже, чем одна эта строка.

Одно исключение из «ордера в конце»: **перед обрывом ставится один лимитный
ордер** минимального размера далеко от рынка. Шагу 18 нужно что-то, что можно
попробовать изменить при мёртвой связи, а размещать ордер во время обрыва нельзя
по построению. Этот ордер — вся цена Частей 1 и 2.

---

# Часть 1 — Биржа на месте, ордеров нет

## Шаг 1. Деск поднимается на Futures

Запустите деск. Выберите рабочее место Futures.

**Смотрим на:** состояние счёта в тикете — балансы, позиции, рабочие ордера,
ALGO-ордера и поток пользовательских данных.

**Должно быть:** все пять доходят до готового состояния. Через несколько секунд
нигде не написано «не прочитано».

**Если что-то не дошло:** запишите точный код (например `-2015`). Код на **всех**
— это креды, права ключа или IP-ограничение. Код только на ALGO-ордерах — это
ALGO-эндпоинт или его право у ключа. Ради этого различия шаг и существует.

**Отдельно запомните ответ по ALGO-ордерам:** если код был только на них, шаг 33
проверять нечем, и это надо будет записать там.

*Закрывает:* `verify-live-futures-account-read` 1.2 и 1.3.

## Шаг 2. Воркстейшн живой

**Смотрим на:** график рисует свечи, стакан заполнен с обеих сторон, лента
печатает сделки.

**Должно быть:** все три двигаются. Это исправление от 2026-08-10 — до него
каждый запрос воркстейшна отклонялся как некорректный, и эти три панели стояли
пустыми.

**Если пусто:** деск снова сломан. Остановите прогон и скажите об этом.

*Закрывает:* подтверждение `isolate-markets-and-runtime` 8.6.

## Шаг 3. Переключение рынков не устраивает шторм

Переключите Spot → Futures → Spot → Futures, не выжидая между переключениями.

**Смотрим на:** терминал, из которого запущен деск.

**Должно быть:** несколько строк. Никакой простыни из повторяющегося
`[market-gate] Refused … generation N is superseded by N+1` и никакого растущего
номера.

**И:** рабочее место, на котором вы остановились, работает — график и стакан
снова наполняются.

*Закрывает:* подтверждение `isolate-markets-and-runtime` 8.5 и 8.8.

## Шаг 4. Док говорит, чего он не знает

Откройте в доке обе вкладки истории: сначала `ORDER HISTORY`, потом
`CLOSED POSITIONS`.

**Смотрим на:** что показано до того, как приедут строки, и что написано в
счётчиках.

**Должно быть:** пока ничего не прочитано, панель говорит, что счёт не прочитан
— а не «0 open» и не «No open positions». Неизвестный счётчик показывается как
`—`.

*Закрывает:* `say-which-readings-are-stale` 5.2, первая половина.

## Шаг 5. Обзор счёта читается один раз

Переключитесь между `ORDER HISTORY` и `CLOSED POSITIONS` по два раза, затем
смените контракт и вернитесь в обзор.

**Должно быть:** таблица не мигает и не пустеет, переключение мгновенное, рядом
с ↻ написано, когда показания сняты. Смена контракта историю не перечитывает —
она принадлежит счёту.

**И отдельно, просмотрев `CLOSED POSITIONS` целиком:** в списке нет позиций,
которых вы не брали. Раньше окно сделок, начавшееся внутри позиции, дорисовывало
«разворот» в противоположную сторону. Строка под таблицей должна называть больше
контрактов, чем вы ожидаете, — поиск начинается с последних суток.

*Закрывает:* `hold-the-history-the-desk-has-read` 5.3 (первая половина) и 8.7
(вторая половина). Вторые половины обоих — шаги 25 и 32.

## Шаг 6. Вёрстка

Посмотрите на деск в том размере окна, в котором работаете, и растянув окно на
весь экран.

**Должно быть:** числа контракта в верхней панели читаются целиком; стакан
целиком в своей панели и **не налезает** на ленту; под таблицей позиций и под
`CLOSED POSITIONS` нет горизонтального скролла; тикет не сплющен до заголовка —
видны кнопки BUY/SELL. Скроллы остались только в списке контрактов, в ленте и в
таблицах дока.

**Отклонение, известное заранее:** тело тикета скроллится, если окно ниже
~1400 px. Это записано в изменении.

Измерено в Chromium на восьми размерах от 1280×720 до 2560×1440: пересечений
панелей нет, горизонтальных переполнений нет.

*Закрывает:* `fit-the-desk-in-the-window` 4.2.

## Шаг 7. Пятнадцать контрактов за минуту не упираются в бюджет

Перещёлкайте **десять-пятнадцать** контрактов за минуту, не дожидаясь полной
загрузки, и обязательно захватите тонкий контракт, у которого стакан часто
пересобирается.

**Должно быть:** ни одного `RESYNCHRONIZING` с причиной `READ_WEIGHT_EXHAUSTED`,
ни одного `UNAVAILABLE`; каждый контракт доезжает до живого состояния. Ни мигания
между двумя монетами, ни зависшего `RESYNCHRONIZING`, и в логе главного процесса
нет `WebSocket was closed before the connection was established`.

Раньше окно вмещало ровно пять переключений в минуту, а одна пересборка стакана
съедала половину окна.

**Если `RESYNCHRONIZING` всё же появится** — запишите **причину** дословно.
`SOCKET_CLOSED` — соединение потеряла биржа; `CONNECTION_ROTATED` — деск сам
переоткрыл сокет по 24-часовому расписанию; `STREAM_FRAME_REFUSED` — деск отбросил
кадр больше своего потолка. Любая другая причина (`SOCKET_NOT_READY`,
`DEPTH_BOOTSTRAP_GAP`, `CANDLE_*`, `READ_WEIGHT_EXHAUSTED`) идёт в отдельную
строку отчёта — они закрывают разные изменения.

`STREAM_FRAME_REFUSED` на живой бирже видеть не должны: на пятиминутном чтении
11.08 самый большой кадр занял 8% потолка.

*Закрывает:* `wait-out-the-read-budget` 4.2, `switch-contracts-without-tearing-the-desk` 5.2.

## Шаг 8. Стакан на всех шагах группировки

На ликвидном контракте пройдите **все** шаги группировки, которые предлагает
контракт, и на каждом посмотрите на стакан. Лестница — 1×, 2×, 5×, 10×, 25×,
50×, 100×, 200×, 500×, 1000× тика, но её конец теперь зависит от контракта:
после `end-the-book-where-the-market-does` она обрывается там, докуда достаёт
книга биржи, поэтому на разных контрактах в списке разное число шагов. Пройдите
те, что есть.

**Должно быть:** то же число строк, те же размеры, тот же накопленный столбец
справа, что и раньше; ни одна сторона не обрывается раньше другой; на самом
грубом шаге строки по-прежнему заполнены до низа панели; после смены шага строки
дозаполняются за мгновение.

**Отдельно — шаг 1×.** На контракте, где уровни стоят редко (цена в тысячных, а
заявки — через каждые несколько тиков), заявленная «дальность» ничего не говорит
о числе строк. Ожидается: на 1× панель заполнена так же, как была.

**Отдельно — смена шага на тихом контракте.** Ожидается: строки перерисовываются
сразу, а не ждут следующего изменения в стакане.

**Отдельно — тонкий контракт при крупном шаге.** Не должно быть строки, которая
занижает объём против стакана в приложении Binance: лучше пустая строка, чем
неполная. Если такая строка встретится — назовите контракт, шаг и время.

Замерено на стенде: кадр стакана 120,9 KiB → 18,0 KiB, работа на один кадр в
обоих процессах 4,90 мс → 1,03 мс. При частоте биржи в 10 кадров в секунду деск
возвращает себе около 39 мс каждой секунды.

*Закрывает:* `send-only-the-book-on-screen` 5.3, `buy-the-book-as-deep-as-it-is-read` 4.2.

## Шаг 9. Стакан отвечает за обе стороны

Откройте контракт, который сильно двигается, и подержите его открытым через
несколько импульсов, поглядывая на бейдж над стаканом.

1. **Короткая сторона не ходит под зелёным бейджем.** Ожидается: либо обе стороны
   заполнены и бейдж `LIVE`, либо сторона короче — и тогда бейдж `STALE`. Чего
   быть не должно: заметно короткой стороны при зелёном `LIVE`. Если увидите —
   снимите скриншот целиком, вместе с бейджем и выбранным шагом группировки.

2. **Книга досбирается за секунду, а не за десять.** Откройте контракт, у
   которого в прошлый раз был выбран грубый шаг (деск помнит шаг по каждому
   контракту отдельно). Засеките на глаз, сколько проходит от появления стакана
   до момента, когда строки заполнены до низа панели. Ожидается: около секунды.
   Раньше это занимало десять–пятнадцать, а на несимметричной книге не
   заканчивалось вовсе.

3. **По уровню `STALE`-книги можно кликнуть.** Поймайте момент, когда бейдж
   `STALE`, и щёлкните по строке стакана. Ожидается: цена подставляется в тикет
   ровно как обычно. Уровни короткой книги точные — их просто меньше. Недоступны
   уровни только тогда, когда книга пришла пустой — кликать не по чему.

4. **Если бейдж `STALE` не уходит.** На неликвидном контракте, читаемом очень
   грубым шагом, биржа может просто не публиковать столько стакана — тогда
   `STALE` честен и останется. Запишите контракт и шаг: это ответ биржи, а не
   дефект деска, но знать про него стоит.

5. **Заодно — стакан падает один.** Если поймаете резкое движение: рабочее место
   НЕ должно уходить в `RESYNCHRONIZING` целиком. Если стакан отвалится — он
   один помечается устаревшим, а цена, свечи, лента и PnL продолжают идти.

Замерено на стенде, на книге, у которой одна сторона в пять раз тоньше другой:
до изменения деск покупал 50 → 500 уровней и **останавливался, так и не покрыв
строки**; после — 50 → 1000 и книга покрыта через 213 мс, а вместе с бутстрапом —
через 218 мс от самого запроса.

*Закрывает:* `prove-the-book-covers-both-sides` 5.3, `hold-the-book-through-a-spike` 5.2.

## Шаг 10. Тихий контракт открывается со стаканом

Откройте **самую неторгуемую** монету, какую найдёте в списке, и просто
посмотрите на неё полминуты.

**Берите именно крипто-контракт.** TBTUSDT, PYPLUSDT, GSUSDT — это
`TRADIFI_PERPETUAL`, перпетуалы на традиционные активы; деск их сознательно не
торгует (`futures-workstation-market-contract.js`, `tradable` требует
`contractType === 'PERPETUAL'`), поэтому на них тикет говорит «Select an active
USDⓈ-M contract» — и это не поломка, а другой разговор. Тип контракта написан в
шапке над названием.

**Должно быть:** обе стороны стакана заполнены **с первого снимка**; лента пишет
«Waiting for aggregate trades…» — и это нормально, на такой монете сделок
действительно нет.

**Чего быть не должно:** пустого стакана, `RESYNCHRONIZING` каждые несколько
секунд и `UNAVAILABLE` в конце. Раньше такая монета не открывалась вовсе: биржа
отдаёт один и тот же снимок, поток молчит, деск считал это поломкой и пересобирал
всю рабочую область восемь раз подряд. Если `RESYNCHRONIZING` всё же появится — в
терминале есть строка `[futures-production-workstation:fault] …`; пришлите её
дословно.

**Про график на этом контракте — шаг 11, не этот.** В заходе 2026-08-12 стакан
здесь прошёл (встал с первого снимка, держался на всех шагах группировки, ни
одного ресинка, контракт ожил за полторы секунды), но пункт был засчитан как
незакрытый, потому что график ушёл в `STALE` и отключил подбор цены, жесты и
подъём ордера мышью. Порог свежести свечей так и остался плоским
(`CANDLES_MS: 5_000`), и на монете без сделок поток свечей молчит минутами — то
есть график здесь **по-прежнему уходит из `live`**. Изменилось другое: с
`let-the-desk-act-on-a-stale-chart` это больше не запрет, а надпись. Поэтому
стакан засчитывается здесь, а поведение графика проверяется отдельным шагом.

*Закрывает:* `bootstrap-the-book-on-a-quiet-market` 4.3.

## Шаг 11. Тихий график: надпись, а не запрет

Тот же тихий контракт, на котором график ушёл из `live`.

1. **График остаётся читаемым.** Дождитесь, пока свечи перестанут приходить.
   Ожидается: полотно **не закрывается** серой шторой. В левом верхнем углу
   появляется маленькая плашка вида `QUIET chart · 47s old`, и счётчик растёт.
   Слово `QUIET`, а не `STALE`: соединение живо, просто по контракту никто не
   торгует. Шкала цен справа не перекрыта.

2. **По тихому графику можно работать.** Щёлкните по цене, сделайте жест
   ALT+right / CTRL+right. Ожидается: цена подставляется в тикет, подтверждение
   открывается у курсора. **Ордер не отправляйте** — на этом шаге проверяется,
   что затвор снят, а не что ордер уходит.

3. **Возраст едет вместе с ценой.** Возьмите цену с тихого графика и доведите до
   подтверждения, но **не отправляйте** — посмотрите на панель и нажмите Esc.
   Ожидается: в тикете под полем «Selected price» и на самой панели подтверждения
   стоит та же плашка с возрастом. Пока панель открыта, число растёт. Если цену
   вы **набрали руками** — никакой плашки быть не должно: это ваше число, у него
   нет возраста.

4. **Штора осталась там, где она честна.** Откройте контракт и посмотрите на
   первую секунду, до первой свечи. Ожидается: вот здесь полотно закрыто, и
   написано, что свечей ещё не приходило. Подбор цены в этот момент недоступен —
   брать нечего.

Пятый случай — что при настоящем обрыве плашка красная и говорит `STALE` /
`DISCONNECTED`, а не `QUIET`, — проверяется в Части 2, шаг 19: там обрыв уже есть.

> 2026-08-12 оператор подтвердил пункт 1 и отказался делать пункты 2 и 3:
> торговать по тихому графику не будет. Пункты 4 и 6 не проходились. Пункты 2 и 3
> оставлены в списке, потому что ордера они не требуют — подтверждение открывается
> и закрывается по Esc, до биржи ничего не доходит.

*Закрывает:* `let-the-desk-act-on-a-stale-chart` 4.2.

## Шаг 12. Быстрый контракт: стакан и лента идут ровно

Возьмите ликвидный контракт, разложите стакан поглубже (двадцать с лишним
уровней в сторону) и смотрите на обе панели минуту.

1. **Обе панели читаются.** Ожидается: идут ровно. Раньше каждый принт
   перестраивал ещё и стакан, который не двигался, а каждое обновление стакана —
   ленту, которая не печатала; на стакане в 24 уровня 152 из 175 форматирований
   чисел, которые стоил один принт, уходили в неподвижный стакан.

2. **Цена на развороте.** Смотрите на последнюю цену в шапке во время быстрого
   движения. Ожидается: цвет меняется в тот же кадр, когда цена развернулась, — не
   на кадр позже.

3. **Последняя цена не стоит при включённом фильтре ленты.** Поставьте в
   настройках ленты минимальный номинал побольше (например 400 USDT), чтобы лента
   почти не обновлялась. Ожидается: `Last` в шапке и строка последней цены между
   сторонами стакана продолжают идти вместе с рынком. Лента показывает ровно то,
   что вы просили — крупные принты.

*Закрывает:* `stop-rebuilding-the-desk-on-every-tick` 5.2 (первая половина),
`move-the-pnl-with-the-market` 5.2 (первый пункт).

## Шаг 13. Спот: график догружается и не тяжелеет

Всё на Spot, на паре, по которой идут сделки.

1. **Прокрутка влево на торгуемой паре.** Отмотайте график влево до самого
   старого бара и остановитесь. Лента при этом должна продолжать печатать.
   Ожидается: подгружаются старые свечи, график становится глубже. Раньше при
   такой скорости ленты он не догружался **никогда** — подписка на диапазон
   пересоздавалась на каждом принте и отменяла собственный таймер ожидания в
   50 мс. Отматывать назад имеет смысл ровно на такой паре, поэтому ломалось это
   именно там, где нужно.

2. **Глубокая история и живая лента.** Догрузите несколько страниц — тысячи
   баров — и оставьте график с идущей лентой на минуту. Ожидается: график не
   тяжелеет от глубины. Раньше каждый принт пересчитывал скользящую среднюю по
   всем барам: на пяти тысячах баров это 0,78 мс чистого счёта на каждый принт,
   помимо отрисовки, и столько же сверху на RSI.

3. **Свеча закрывается.** Дождитесь смены свечи на минутном интервале.
   Ожидается: новая свеча появляется, а закрывшаяся показывает свои настоящие
   максимум, минимум и объём.

4. **RSI не уезжает.** Во время движения и после того, как несколько свечей
   закрылись. Ожидается: линия стоит там же, где стояла бы при полном пересчёте.
   Сглаживание Уайлдера рекурсивно от первого бара, и неверный перенос состояния
   не падает, а тихо уводит индикатор; он сверен с полным расчётом на трёхстах
   шагах, включая случай, где делитель уходит в ноль. Если увидите расхождение с
   TradingView, которого раньше не было, — это то место.

5. **Месяц.** На паре с длинной историей переключите интервал на `1M` и
   прокрутите влево до подгрузки. Ожидается: свечи за прошлые годы подгружаются и
   **остаются** на графике при дальнейшей прокрутке. До исправления месяц
   считался за ровно 30 дней, и на каждом шве через 31-дневный месяц — в 2023
   году таких швов шесть из одиннадцати — уже загруженная история выбрасывалась
   целиком.

   Отдельно, если случай подвернётся: последняя свеча месячного графика в течение
   минуты после начала нового месяца должна открыться первым числом. Не
   подвернётся — так и напишите, шаг не блокирующий.

*Закрывает:* `stop-rebuilding-the-desk-on-every-tick` 5.2 (вторая половина),
`keep-the-chart-loadable` 6.3.

## Шаг 14. Журнал деска

При запуске деска в терминале появляется строка `[Electron] Desk record: …` —
это путь к каталогу журнала (на Linux обычно `~/.config/cc-trade/diagnostics`).
Не закрывая деск, выполните из корня репозитория:

```
node scripts/read-desk-record.mjs
```

**Должно быть:** сводка за день — сколько чего произошло, какие коды встретились,
с какой причиной был каждый `RESYNCHRONIZING`, какие фазы были самыми медленными,
во сколько деск запускался и останавливался.

Затем откройте сам файл `desk-<дата>-000.jsonl` глазами и убедитесь, что **ни в
одной строке нет**: ключа или подписи, цены, объёма, суммы в USDT, PnL. Ордер в
журнале выглядит как контракт / сторона / тип / идентификатор / чем кончилось — и
больше ничего. Если найдёте хоть одну строку с суммой или с чем-то похожим на
ключ — пришлите её дословно, это ошибка, а не настройка.

Время в журнале **UTC**, не местное. Это сделано намеренно — вопросы к бирже
задаются в её дне.

*Закрывает:* `keep-a-record-of-what-the-desk-did` 7.3.

---

# Часть 2 — Один обрыв на весь прогон

Эти шаги останавливают локальный прокси, и деск перестаёт видеть Binance. Сам
деск при этом работает. Ничего не размещается и ничем не рискуем; единственное
следствие — пока прокси не вернётся, счёт не читается.

**Перед началом:**

- Убедитесь, что нет позиции, которую вам может понадобиться закрыть в ближайшие
  пять минут.
- **Поставьте один лимитный ордер минимального размера далеко от рынка и
  оставьте его рабочим.** Он нужен шагу 18 и отменяется на шаге 21. Исполниться
  он не должен.

## Шаг 15. Отрезать биржу

Остановите прокси на `127.0.0.1:1080`. Засеките время.

Дальше — шаги 16, 17, 18, 19 и 20 подряд, **не выходя из обрыва**.

> **Пока идёт обрыв — не трогайте деск сверх того, что написано в шагах.** Не
> переключайтесь на Spot и обратно, не выбирайте другой контракт, не нажимайте
> `Retry`. Любое из этого пересобирает сессию и **обнуляет счётчик попыток**,
> после чего лестница переподключений начинается заново и до потолка не доходит —
> проверять шагом 20 становится нечего. Это не теория: 2026-08-13 первый прогон
> был испорчен ровно так. В журнале деска обрыв длился 104 секунды, но успешной
> оказалась восьмая попытка **новой** лестницы, с паузами 0,5 → 1 → 2 → 4 → 8 →
> 16 → 30 с; старый код дошёл бы до неё тоже.
>
> Как убедиться, что проверка состоялась: в журнале между началом обрыва и
> `state: "live"` должно стоять **подряд больше восьми** переходов в
> `resynchronizing`, без единого `loading` с паузой меньше предыдущей. Пауза
> 30 с, повторённая дважды и более, — это и есть то, чего раньше не было.
>
> Прокрутка графика (шаг 19), открытие панели изменения ордера (шаг 18) и чтение
> тикета сессию не пересобирают — эти шаги внутри обрыва делать можно.

## Шаг 16. Замерзшая цена не выдаётся за рыночную

Примерно через тридцать секунд после обрыва.

**Смотрим на:** mark-цену и нереализованный PnL по любой открытой позиции, и на
терминал.

**Должно быть:** в терминале сообщение, что поток mark price ничего не доставил
за 15 секунд. Значения позиции опираются на последнее чтение счёта, а не стоят на
цене, которая перестала двигаться. Ничто на экране не выдаёт за текущую рыночную
цену то, чего у него нет.

*Закрывает:* `say-which-readings-are-stale` 5.2, вторая половина.

## Шаг 17. Неудачное чтение счёта заявляет о себе

**Смотрим на:** тикет и док.

**Должно быть:** ресурсы счёта говорят, что чтение не удалось — с кодом, — а не
показывают последние значения как текущие. Контрол размера ордера отказывается
считать размер и объясняет почему.

**Запишите:** точную формулировку отказа. Он должен называть причину, а не просто
отказывать.

Заход 2026-08-13 дал здесь `PASS`: «The Futures account request could not reach
Binance. Check network and proxy settings, then retry.» / «LONG ENTRY NOT SENT».

## Шаг 18. Панель, которая не смогла отправить, не закрывается

*Риск: нулевой. Команда намеренно не доходит до биржи.*

Откройте панель изменения того ордера, который вы поставили перед обрывом,
поменяйте цену, отправьте.

**Должно быть:** панель **остаётся открытой** и говорит, что ордер не был
изменён. Она не должна закрыться — исчезнувшая панель читается как «готово».

Закройте панель. Ордер оставьте — он отменяется на шаге 21.

*Закрывает:* `send-only-the-confirmed-order` 5.2, вторая половина.

## Шаг 19. Неудачное чтение истории не запирает Futures-график

Всё ещё без биржи, прокрутите Futures-график влево, пока он не запросит более
старые свечи.

**Должно быть:** жёлтая строка в левом нижнем углу графика — старые свечи
загрузить не удалось. Она **не исчезает сама**: остаётся до тех пор, пока
следующая прокрутка влево не загрузит страницу. Это намеренно — исчезающая строка
оставляет график выглядящим так, будто у контракта просто больше нет истории.

**И заодно, на плашке состояния графика:** при настоящем обрыве она красная и
говорит `STALE` / `DISCONNECTED`, а не `QUIET`. Слово `QUIET` деск говорит только
тогда, когда соединение живо и молчит один поток. Это пятый пункт шага 11.

**Теперь верните прокси и прокрутите влево ещё раз** — это то, что в прошлый раз
не сработало: свечи не загрузились никогда, а строка продолжала предлагать
прокрутить снова. Ожидается одно из двух, и оба — правильный ответ:

1. **Свечи подтягиваются**, строка исчезает. Так и должно быть в обычном случае.
2. **Строка меняет слова**: вместо «scroll again to retry» — «No older candles»,
   и дальше либо «this contract’s history starts here» (у контракта больше нет
   истории), либо «the chart holds as far back as it draws» (упёрся сам деск,
   он рисует ограниченный отрезок). В обоих случаях деск больше не просит
   прокрутить: прокрутка ничего не даст, и он об этом говорит.

**Ошибка, если:** строка по-прежнему говорит «scroll again to retry», а прокрутка
влево ничего не грузит. Тогда скажите — это ровно тот дефект, который чинили
(`let-the-chart-ask-again`), и он не закрылся.

*Закрывает также:* `let-the-chart-ask-again` 3.3.

**И заодно, на тикете:** ордер по-прежнему можно довести до подтверждения, и
подтверждение говорит, что график недоступен и возраст показаний неизвестен. Это
сделано намеренно (`let-the-desk-act-on-a-stale-chart`), а не забыто. Если
подтверждение молчит о том, что рынок мёртв — скажите, это уже дефект.
**Отправлять не надо.**

*Закрывает:* `keep-the-chart-loadable` 6.2 (Futures), `let-the-desk-act-on-a-stale-chart` 4.2 п.5.

## Шаг 20. Рынок возвращается сам после долгого обрыва

Из `recover-the-market-feed-after-an-outage`. Это прямой ответ на то, что нашлось
в этом же прогоне: после короткого обрыва прокси (1–2 минуты) деск оживал сам, а
после долгого — график, стакан и лента не возвращались никогда, и помогал только
`Ctrl+R`.

Причина: лестница переподключений тратила 91,5 секунды (0,5 + 1 + 2 + 4 + 8 + 16 +
30 + 30) и после этого сессия **останавливалась насовсем**. Счётная часть деска в
это же время переподключается без всякого потолка — поэтому позиция, кошелёк и
uPnL продолжали жить, и деск выглядел здоровым, будучи слепым. Теперь лестница
кончает спешку, а не восстановление: деск продолжает стучаться раз в 30 секунд,
сколько бы обрыв ни длился.

1. **Пока рынка нет, деск говорит об этом на графике.** После первых полутора
   минут обрыва посмотрите на график.

   Ожидается: поверх графика, сверху по центру, красная строка «Market data
   stopped after repeated reconnect failures — still retrying.» и рядом кнопка
   `Retry now`. Раньше единственная кнопка `Retry` жила в списке контрактов и
   говорила про контракты, а не про рынок — поэтому её и не нажали.

2. **Обрыв длиннее двух минут — деск возвращается сам.** — `PASS`, проверено
   2026-08-13 с третьего захода.

   Дождитесь, пока с начала обрыва пройдёт **не меньше двух минут** (потолок
   лестницы — 91,5 секунды). Затем включите прокси и **не трогайте ничего**.

   Ожидается: в течение примерно полуминуты после включения график, стакан и
   лента оживают **сами**. `Ctrl+R` не нужен. Засеките, сколько прошло, и
   запишите.

   Как прошло 2026-08-13: обрыв в 15:25:32, паузы 1 → 2 → 4 → 8 → 16 → 30 → 30 с,
   потолок `RECONNECT_EXHAUSTED` в 15:27:03.62 — через 91,4 секунды. **Девятая
   попытка ушла в 15:27:33.62, ровно через 30,1 секунды после потолка**, то есть
   по собственному таймеру деска: оператор ничего не нажимал. Рынок вернулся в
   15:27:35.36. Девятой попытки в старом коде быть не могло: на потолке сессия
   уничтожалась вместе со всеми таймерами. Это и есть доказательство изменения —
   не время восстановления, а сам факт, что после потолка попытка была. Со стороны
   экрана: позиции вернулись за 4–5 секунд (счётная часть, у неё потолка нет),
   всё остальное — примерно за 18 секунд после включения прокси.

3. **Кнопка `Retry now` работает.** — `PASS`, проверено 2026-08-13. Повторять не
   нужно; если всё же захотите — это **отдельный** обрыв, потому что нажатие
   пересобирает сессию и обесценивает пункт 2.

   Как прошло: обрыв в 15:18:21, лестница выдала паузы 1 → 2 → 4 → 8 → 16 → 30 →
   30 с и упёрлась в потолок ровно через **91 секунду**. Оператор увидел строку и
   кнопку на графике, вернул прокси, нажал `Retry now`, и попытка ушла в
   15:20:17.48 — на **5,3 секунды раньше**, чем сработал бы собственный таймер
   деска. Рынок вернулся в 15:20:19.06. Это доказывает кнопку и **не** доказывает
   пункт 2: нажатие опередило самостоятельное восстановление на пять секунд.

Плата: контракт, который биржа никогда не отдаст, деск будет спрашивать раз в
30 секунд, пока он выбран. Это один запрос в полминуты — дешевле, чем деск,
который замолчал навсегда.

*Закрывает:* `recover-the-market-feed-after-an-outage` 4.2.

## Шаг 21. Второй, короткий обрыв: Spot-график

Теперь, когда шаг 20 отмерян, переключаться можно.

Переключитесь на **Spot**. Остановите прокси ещё раз. Прокрутите график влево,
пока он не запросит более старые свечи.

**Должно быть:** сообщение, что старые свечи загрузить не удалось.

Верните прокси. Дождитесь, пока счёт прочитается снова (несколько секунд).
Прокрутите влево ещё раз.

**Должно быть:** загружается. До исправления одно неудачное чтение отключало
прокрутку влево до конца сессии.

Это заодно контрольный случай к шагу 19: Spot ходит за историей другим путём и
восстанавливался всегда. Если здесь загружается, а на Futures в шаге 19 нет —
дефект остался на стороне Futures, и разница видна прямо в этой паре.

Вернитесь на Futures и **отмените ордер**, поставленный перед шагом 15.

*Закрывает:* `keep-the-chart-loadable` 6.2 (Spot), `let-the-chart-ask-again` 3.4.

---

# Часть 3 — Настоящие ордера

Дальше деск размещает настоящие ордера. Каждый шаг говорит, чем именно вы
рискуете. Общая цена всей части — раздел «Во что обойдётся».

Перед началом убедитесь, что `FUTURES_MAX_ORDER_USDT` выставлен.

## Шаг 22. Уходит то, что было подтверждено

*Риск: один лимитный ордер минимального размера, далеко от рынка, исполниться не
должен. В конце шага вы его отменяете.*

1. Выберите размер в тикете.
2. Начните ордер и прочитайте панель подтверждения. **Запишите количество и цену,
   которые в ней показаны.**
3. Оставьте панель открытой на 10–15 секунд — чтобы под ней успело приехать хотя
   бы одно обновление счёта.
4. Подтвердите.

**Должно быть:** ордер, появившийся в списке рабочих, имеет ровно то количество,
которое вы записали. Не пересчитанное.

5. Отмените ордер.

*Закрывает:* `send-only-the-confirmed-order` 5.2, первая половина.

## Шаг 23. Ордера на других контрактах остаются в списке

*Риск: один лимитный ордер минимального размера на втором контракте, далеко от
рынка. Отменяется в конце шага. **Если у вас уже висит рабочий ордер на любом
другом контракте — не ставьте новый, риск нулевой.***

Если своего ордера на втором контракте нет — поставьте. Переключите выбранный
контракт.

**Должно быть:** ордер на другом контракте всё ещё в списке рабочих ордеров.
Список принадлежит счёту, а не контракту.

Отмените его, если ставили.

*Закрывает:* `verify-live-futures-account-read` 1.4. Оговорка про «не ставьте,
если уже есть» стоит здесь не для экономии: задача 1.5 того же изменения требует,
чтобы **его** проверка не разместила ни одного ордера, и наблюдение 1.4 этого не
требует — ему нужен любой ордер, чей он, неважно. Шаг ставит свой только потому,
что прогон не должен зависеть от того, что у оператора уже открыто.

## Шаг 24. Отказ называет код биржи

*Риск: нулевой — ордер, который биржа обязана отклонить, на бирже не появляется.*

Выставьте ордер, который биржа обязана отклонить: проще всего размером ниже
минимума контракта (нотионал меньше 5 USDT). Деск покажет отказ на экране, как и
раньше. Затем прочитайте день:

```
node scripts/read-desk-record.mjs
```

**Должно быть:** в сводке появился раздел **`Refusals by the code the exchange
gave`**, и в нём стоит код биржи — например `-4164` (нотионал ниже минимума) или
`-1013` (нарушен фильтр контракта), а не только `FUTURES_API_ERROR`. Раньше на
этом месте было бы `(the exchange stated none)`.

Затем откройте строку отказа в самом файле и проверьте, что **текст ошибки от
биржи в неё не попал**: должен быть только код. Строка вида «Order's notional must
be no smaller than 20» в журнале — это ошибка, потому что она цитирует сумму;
пришлите её дословно.

Записи, сделанные до этого изменения, останутся как есть — у них кода нет, и в
сводке они честно числятся как `(the exchange stated none)`.

*Закрывает:* `name-the-refusal-the-exchange-gave` 4.4.

## Шаг 25. Ордер появляется один раз и остаётся

*Риск: три лимитных ордера минимального размера далеко от рынка (отменяются) и
**один ордер, который исполнится** — поставьте его у самой цены.*

Ордер после постановки несколько раз пропадал и появлялся: поток сообщал о нём за
миллисекунды, а чтение `/fapi/v1/openOrders`, ушедшее раньше, возвращалось без
него и стирало. Следующее чтение возвращало обратно.

1. **Поставленный ордер появляется один раз.** Поставьте лимитный ордер
   шорткатом и смотрите на график и на список рабочих ордеров десять-пятнадцать
   секунд. Ожидается: линия появляется один раз и стоит. Ни одного мигания.

2. **То же при нескольких ордерах подряд.** Поставьте два-три ордера подряд, не
   выжидая между ними. Ожидается: каждый появляется и остаётся; ни один не
   исчезает, когда появляется следующий.

3. **Исполненный уходит.** Поставьте лимитный ордер у самой цены и дождитесь
   исполнения. Ожидается: уходит из списка сразу, без ↻.

4. **Чужая отмена по-прежнему видна.** Отмените один из оставшихся ордеров **из
   приложения Binance** (не с деска). Ожидается: пропадает с деска сразу — отмену
   приносит тот же стрим, и она снимает удержание, не дожидаясь окна.

5. **Ордера, о которых стрим молчит.** Если есть ордер, поставленный давно и не
   трогавшийся, снимите его снаружи при упавшем стриме. Ожидается: пропадает на
   первом же чтении после окна — окно короткое (3 с) и отсчитывается от
   последнего слова стрима, а не от чтения. Если случая нет — `NOT OBSERVABLE`.

6. **Если ордер вернулся в список сам собой** — это ровно тот случай, ради
   которого написана память об уже закрытых; запишите время и contract/orderId.

Отмените всё, что осталось.

Плата: ордер, снятый снаружи так, что стрим об этом не сказал, проживёт на экране
до трёх секунд дольше. Обратная сторона — та, из-за которой это и делалось: ордер,
который биржа уже приняла, больше не исчезает с экрана.

*Закрывает:* `hold-the-order-the-read-has-not-seen` 3.2,
`hold-the-working-orders-on-the-stream` 5.2.

## Шаг 26. Быстрая серия ордеров не выключает торговлю

*Риск: 5–6 лимитных ордеров минимального размера далеко от рынка, все
отменяются.*

Причина изменения найдена по вашему же логу: каждая команда заканчивалась полным
перечитыванием счёта — четыре подписанных запроса, 90 из 800 весов в минуту. Один
запрос к Binance через ваш прокси — 340–800 мс, то есть проход занимал
полторы-две с половиной секунды, и всё это время гейт готовности считал, что
баланса нет. Отсюда `LONG entry NOT sent — Loading Futures account state`,
мигающая панель размера и бейдж `SYNC`. Дальше кончался вес: 90 на команду — это
восемь команд в минуту.

1. **Быстрая серия.** Поставьте подряд 5–6 лимиток шорткатом, не делая пауз, и
   пробуйте сразу после каждой поставить следующую.

   Ожидается: ни одного `NOT sent` с текстом про `Loading Futures account state`.
   Панель размера не гаснет и не обнуляется, бейдж не уходит в `SYNC`.

2. **Драг сразу после постановки.** Поставьте ордер и немедленно потяните его.
   Ожидается: тянется сразу, реплейс уходит без отказа.

3. **Каждый ордер появляется один раз.** Смотрите на список во время серии.
   Ожидается: появляется один раз и остаётся.

4. **Отвал стрима — обратная сторона.** Если хотите проверить: остановите прокси,
   дождитесь, пока пометится потеря стрима, верните прокси и поставьте ордер.
   Ожидается: при отсутствии стрима деск после команды перечитывает счёт целиком —
   иначе ему неоткуда узнать, что команда сделала. Это отдельный короткий обрыв;
   если делать не хотите — `NOT OBSERVABLE`.

Отмените все ордера серии.

Плата: команду, о которой стрим почему-то промолчал, теперь исправляет
периодическая сверка (десятки секунд), а не чтение сразу после.

*Закрывает:* `keep-trading-while-the-account-is-read` 4.2.

## Шаг 27. Драг: ордер идёт за рукой

*Риск: один лимитный ордер минимального размера далеко от рынка. Каждое
перетаскивание — это отмена и новая постановка, то есть за шаг проходит около
семи постановок одного и того же ордера. В конце отменяется.*

Раньше ордер не двигался, пока биржа не подтвердит отмену — те самые
полсекунды-секунда паузы. Теперь жест идёт сразу, а отмена летит рядом с ним.
Каждый раз сверяйтесь со стаканом биржи.

1. **Ордер идёт за курсором сразу.** Возьмите рабочий ордер (Ctrl или Alt + левая
   кнопка) и ведите. Ожидается: метка начинает двигаться в тот же момент, без
   паузы; идёт за курсором без отставания и не «догоняет» его после остановки.
   Поводите быстро и размашисто, и во время активного рынка.

2. **Пока биржа не ответила, ордер виден там, где лежит.** В первые полсекунды
   посмотрите на исходную цену. Ожидается: на старом уровне стоит сам ордер с
   подписью `lifting…`, а под курсором — бледная пунктирная метка без подписи на
   шкале. Как только биржа подтвердит отмену, старая метка исчезает, метка под
   курсором становится обычной — яркой, с ценой на шкале, — и на покинутом уровне
   появляется тонкая точечная риска.

3. **Как выглядит метка.** В момент перетаскивания метка у курсора выглядит как
   обычная ручка ордера — тот же размер шрифта, та же табличка, только пунктиром;
   ничего не вылезает за её рамку и вторая рамка вокруг ручки не рисуется.

4. **Быстрый бросок доезжает.** Возьмите ордер и отпустите **раньше**, чем
   пропадёт старая метка — то есть до ответа биржи. Ожидается: ордер
   переставляется туда, куда вы его бросили, а не возвращается на старое место.

5. **Отпущенный модификатор отменяет жест.** Возьмите ордер и отпустите Ctrl (или
   Alt), не отпуская кнопку мыши, пока старая метка ещё видна. Ожидается: метка
   перестаёт идти за курсором сразу, и когда биржа ответит, ордер встаёт обратно
   на исходную цену.

6. **Драг после смены размера панели.** Потяните разделитель панелей, поменяв
   высоту графика, и сразу возьмите ордер. Ожидается: метка встаёт под курсор, а
   не со смещением.

7. **Бросок выше потолка.** Поднимите ордер и бросьте заведомо выше
   `FUTURES_MAX_ORDER_USDT`. Ожидается: алерт «Order NOT moved» с причиной, и
   ордер **возвращается сам** на ту цену, где лежал. Нажимать ничего не нужно.
   Раньше здесь было «Order cancelled and NOT replaced» и кнопка «Place it again
   at …» — ордера до нажатия не было ни на бирже, ни на графике.

8. **Бросок ниже минимума биржи.** Это то, на чём в прошлый раз пропал живой
   ордер: 23 по 0.2029 — 4,67 USDT против минимума в 5, отмена прошла в 18:12:27,
   постановка отклонена в 18:12:29 с `-4164`.

   Возьмите ордер минимального размера (нотионал у самого минимума — 5 USDT) и
   тащите **вниз**: цена падает, количество остаётся, значит нотионал уходит под
   минимум. Отпустите.

   Ожидается: алерт «Order NOT moved» со словами «below the Binance minimum
   notional of 5 USDT», и ордер стоит там, где стоял. **Сверьтесь со стаканом
   биржи** — он должен быть на бирже, а не только нарисован.

   **Ошибка, если:** видите «Order cancelled and NOT replaced» и ордера на бирже
   нет. Скажите — это `do-not-cancel-what-cannot-be-replaced`, и он не закрылся.

   И ещё одно, тем же жестом: если ордер лежит **уже** ниже минимума (например,
   рынок ушёл), поднять его не должно получиться вовсе — «Order NOT lifted … The
   order was left where it is.», и отмена не уходит. Ордер остаётся живым.

9. **Отказ биржи ничего не ломает.** Если отмену отклонили (например, ордер успел
   исполниться), метка под курсором пропадает, ордер остаётся на своём уровне, и
   деск говорит причину.

Отмените ордер.

Что осталось прежним: сам обмен с биржей не изменился — по-прежнему отмена, а
потом постановка. Окно между ними и всё, что деск про него говорит, те же. Отмена
по-прежнему уходит в момент подъёма, до того как известна цена броска, — деск
теперь отказывает **броску**, а не ордеру. И то, чего деск заранее знать не мог,
по-прежнему кончается словами «cancelled and not replaced» с кнопкой возврата:
эта строка никуда не делась, она перестала быть ответом на случай, который деск
мог поймать сам.

*Закрывает:* `start-the-drag-on-the-pointer` 4.2, `cancel-the-order-the-drag-lifts` 6.2,
`do-not-cancel-what-cannot-be-replaced` 3.4.

## Шаг 28. Одна команда — одна отправка

*Риск: три лимитных ордера минимального размера далеко от рынка, все отменяются.*

Между командой и биржей встала очередь: команды по **одному ордеру** идут строго
по одной, а команду с уже виденным идентификатором деск не отправляет второй раз —
отвечает тем, что записал в первый. Команды, которые говорят за весь контракт —
«отменить все», плечо, тип маржи, довнесение маржи, — идут по контракту: они
выполняются в одиночку, ничего на этом контракте их не обгоняет.

*Очередь была по контракту и сузилась до ордера в
`lift-the-next-order-while-the-last-lands` §1c: широкая очередь заставляла отмену
следующего ордера ждать постановку предыдущего, и три ордера рядом было не
подвинуть. Пункт 2 ниже — про один и тот же ордер — от этого не изменился.*

Проверять здесь нечего в смысле «увидеть новое»: если всё работает, деск ведёт
себя ровно как раньше. Проверять нужно обратное — что **ничего не встало**.

1. **Обычная торговля не замедлилась.** Поставьте лимитный ордер, подвиньте его
   через панель редактирования, отмените. Ожидается: каждое действие отвечает так
   же быстро, как до изменения. Насторожить должно другое: команда, которая
   «висит» и отвечает через несколько секунд после следующей.

2. **Две команды подряд по одному ордеру.** Подвиньте ордер и сразу, не
   дожидаясь ответа, отмените его. Ожидается: отмена срабатывает — просто после
   того, как биржа ответила на перенос. Ордер остаётся отменённым, а не
   возвращается перенесённым.

3. **Два контракта не мешают друг другу.** Отмените ордер на одном контракте и
   тут же поставьте ордер на другом. Ожидается: второй не ждёт первого.

4. **Два ордера на одном контракте не мешают друг другу.** Отмените один и тут же
   поставьте второй. Ожидается: второй не ждёт первого. Это то, что сузилась
   очередь; подробная проверка перетаскиванием — шаг 40.

Плата, которую изменение берёт сознательно: если биржа не ответила и деск ушёл в
сверку, следующая команда **по тому же ордеру** ждёт её конца. Потолок считается:
каждый REST-запрос обрывается на 10 с, сверка делает три попытки с паузами 0,5 и
1 с — худший случай около **40 секунд**. По другим ордерам и другим контрактам в
это время всё работает. Это выбрано намеренно: отмена, пришедшая позже, лучше
отмены, которая обгонит постановку и оставит ордер живым.

У «отменить все» это окно шире: она держит весь контракт, и пока она в сверке,
ни один ордер на нём не поставить и не подвинуть. Так и задумано — постановка,
обогнавшая сметание, оставила бы ордер живым на книге, которую вы считаете
пустой. Но окно длинное: если поймаете его вживую, скажите.

*Закрывает:* `serialize-and-deduplicate-trading-commands` 3.2.

## Шаг 29. «Отменить всё» убирает и условную книгу

*Риск: один лимитный и один стоп-ордер минимального размера, оба отменяются
внутри шага.*

**Где кнопка.** Она одна и она **по контракту**: внизу тикета, подписана
`Cancel all <КОНТРАКТ>` — например `Cancel all APRUSDT`. Отдельной кнопки «снять
всё по счёту» в деске нет и не планировалось: отмена всего по счёту одним
нажатием — не та команда, которую стоит держать под пальцем на живом счёте.
Проверяется именно контрактная.

1. Выберите один контракт и разместите на нём лимитный ордер далеко от рынка.
2. На **том же** контракте разместите стоп-ордер (условный), тоже далеко.
3. Убедитесь, что оба видны в списке рабочих ордеров.
4. Нажмите `Cancel all <КОНТРАКТ>` внизу тикета.

**Должно быть:** оба исчезли с деска. Ордера на **других** контрактах остались
на месте — кнопка не трогает их, и это не дефект.

5. **Теперь откройте сам Binance** — сайт или приложение, открытые ордера по
   этому контракту.

**Должно быть:** там тоже пусто. До исправления деск отменял только обычную
книгу, и стоп оставался живым на бирже под пустым списком на экране. Это самый
важный шаг всей части.

*Закрывает:* `answer-the-command-that-asked` 4.2, вторая половина. Первая
половина этой задачи — в разделе «Чего этот прогон закрыть не может».

## Шаг 30. Ордер, который исполнился — от начала до конца

*Риск: один рыночный ордер минимального размера, который исполнится, и его
закрытие. Позиция стоит открытой столько, сколько вам нужно на пункты 2–5. Цена —
комиссия за две сделки плюс движение рынка за это время.*

1. Разместите рыночный ордер минимального размера.

2. **Позиция появляется вместе с филом, а не после него.** Смотрите на строку
   позиции и на кошелёк. Ожидается: размер, средняя и баланс встают на место
   сразу — это тот же кадр, которым биржа сообщила о филе. Раньше между филом и
   строкой был один подписанный круг, 340–800 мс через ваш прокси.

3. **Линия ликвидации.** На только что открытой позиции. Ожидается: несколько
   сотен миллисекунд LIQ нет, потом она появляется — биржевая. Её нет ни в одном
   стриме, и считать её самим деск не будет: неверная линия ликвидации хуже
   отсутствующей ровно там, где она нужна.

4. **PnL движется, а не шагает.** Марк-прайс Binance публикует раз в секунду —
   быстрее нет. Между двумя марками позиция переоценивается по последней цене
   сделки. Ожидается: uPnL меняется плавно, а не рывками раз в секунду. Пока
   цифра посчитана по последней сделке, сумма подчёркнута точечной линией; как
   только приходит марк — подчёркивание пропадает, это подтверждённое число
   биржи. Наведите на саму сумму — в подсказке стоит число биржи по марку на тот
   же момент; на заголовок колонки `uPnL (ROE)` — там правило записано словами.

5. **Подтверждённая цифра сходится с Binance.** В момент, когда подчёркивания
   нет, сравните uPnL с приложением Binance. Ожидается: совпадает. Подчёркнутая
   оценка сходиться не обязана — это цена последней сделки, а не марк.

6. **Ликвидация и маржа не оцениваются.** Смотрите на колонку `Liq.` во время
   движения, а потом откройте панель маржи на изолированной позиции. Ожидается:
   `Liq.` стоит на марке и не дёргается вместе с оценкой. В панели маржи запас до
   ликвидации и сумма, которую деск разрешает вывести, тоже считаются от марка.
   Это нашлось на аудите: сначала они шли за оценкой, то есть деск мог предложить
   вывести маржу, которой у позиции нет.

7. **Плечо.** Нажмите ↻ несколько раз подряд. Ожидается: множитель рядом с
   позицией остаётся верным. Затем смените плечо **с деска** — обязано смениться
   сразу; и смените плечо **в приложении Binance** — обязано подтянуться либо в
   течение десяти минут само, либо сразу, если выбрать этот контракт на деске.

8. **Закрытие позиции.** Закройте её целиком. Ожидается: строка уходит сразу,
   позиции по другим контрактам не трогаются.

9. **Обзор подхватывает фил сам.** Откройте `CLOSED POSITIONS`, **не нажимая ↻**.
   **Должно быть:** одна строка на только что сделанный круг, со входом, выходом
   и реализованным PnL — уже в списке, без обновления. Не две строки и не
   полупустая. То же самое в `ORDER HISTORY`: исполненный ордер там, и ↻ вы не
   нажимали. Это вторая половина проверки «обзор читается один раз» — первая была
   шагом 5.

10. Откройте `ORDER HISTORY`. **Посмотрите и запишите честно, можете ли вы
    понять, что стало с каждым ордером, не прокручивая вбок.** Это «как было» для
    `make-the-order-history-readable` — оценка, а не проверка.

11. **Фандинг** — если случай подвернётся. Посмотрите на кошелёк в момент
    00/08/16 UTC. Ожидается: баланс меняется, и никакого чтения позиций за этим не
    следует — двигался только кошелёк. Не подвернётся — `NOT OBSERVABLE`.

Плата: до секунды на экране может стоять число, которого нет у биржи. Оно
помечено, и марк его заменяет. Если подчёркивание мешает читать — скажите.

*Закрывает:* `let-the-stream-state-the-account` 4.2 и 4.3,
`move-the-pnl-with-the-market` 5.2, `read-a-contract-configuration-once` 4.2,
`hold-the-history-the-desk-has-read` 5.3 (вторая половина).

## Шаг 31. Исполнение впереди рыночных данных

*Риск: один лимитный ордер минимального размера, который **исполнится** во время
быстрого движения, плюс одна отмена и одна постановка на том же движении.*

Про то, что «нажал на пике, а оно ушло позже». Проверять на **ликвидном
контракте во время быстрого движения** — в тихом рынке разницы не будет по
построению.

1. **Заполнение уходит из списка сразу, а не после импульса.** Поставьте лимитку
   так, чтобы она исполнилась в момент, когда стакан и лента идут полным ходом.
   Смотрите на список рабочих ордеров и на отметку на графике. Ожидается:
   исполненный ордер исчезает из списка тогда же, когда исчез бы в спокойном
   рынке. Раньше он ждал позади каждого стакана, который деск успел отправить за
   время всплеска. Если заметите задержку — скажите, на каком контракте и
   насколько примерно.

2. **Отмена и постановка отвечают в том же темпе.** На том же движении отмените
   стоящий ордер и поставьте новый. Ожидается: подтверждение приходит без
   ощущения, что деск «думает».

3. **Стакан на всплеске не отстаёт по-старому.** Ожидается: если деск не
   успевает, он показывает **последнюю** книгу, а не очередь из старых. Заметно
   это должно быть как пропуск кадра, а не как отставание, которое накапливается.

4. **Если деск закрыл соединение сам.** В крайнем случае — окно, которое совсем
   перестало забирать данные, — деск закрывает соединение, и оно переподключается
   с чтением счёта заново. Это ожидаемо, но редко: если увидите переподключение на
   ровном месте, запишите время, оно сопоставится с журналом.

Закройте позицию, если она открылась.

Замерено на стенде, на книге в 88,3 КиБ (тысяча уровней на сторону): чтение
одного доставленного кадра в рендерере 2,956 → **0,736 мс**, отправка одного
события из главного процесса 0,272 → **0,141 мс**. Заполнение, выпущенное в
секундный затор при десяти книгах в секунду, раньше уходило позади **10 книг и
0,86 МиБ**, и рендерер разбирал их все — **29,6 мс** — прежде чем дойти до него.
Теперь впереди него не идёт ничего.

*Закрывает:* `carry-execution-ahead-of-market-data` 5.4.

## Шаг 32. Закрытая позиция остаётся закрытой

*Риск: два рыночных ордера минимального размера на том же контракте — открыть и
закрыть. Две сделки.*

Закройте позицию на любом контракте, убедитесь, что она появилась в
`CLOSED POSITIONS`, **переоткройте позицию на том же контракте** и нажмите ↻.

**Должно быть:** старая запись на месте, с прежним PnL; новая позиция в закрытых
НЕ значится, пока не закрыта.

Закройте новую позицию.

*Закрывает:* `hold-the-history-the-desk-has-read` 8.7 (первая половина). Вторая
половина — просмотр списка целиком — сделана на шаге 5.

## Шаг 33. Сработавший ALGO перестаёт быть рабочим ордером

Из `name-the-algo-order-that-fired`, §4.3. Переписано после аудита изменения —
первая редакция обещала больше, чем изменение делает, и просила проверять именно
тот случай, который оно не закрывает.

*Риск: один `STOP`/`TAKE_PROFIT` с лимитной ценой, который **должен сработать**.
Это единственный шаг, где момент исполнения выбираете не вы. Одна сделка по
порождённому лимитнику.*

**Предпосылка.** Шаг 1 должен был пройти по ALGO-ордерам. Если там был код только
на них — эти шаги проверять нечем, так и запишите.

**Чего изменение НЕ делает.** Оно не сокращает время от срабатывания стопа до
момента, когда деск об этом узнал. Деск не узнаёт об алго-ордерах из стрима, а
связь «этот обычный ордер породил вон тот алго» существует только внутри ответа
биржи на чтение списка алго — то есть приходит на той же тридцатисекундной
сверке. Для `STOP_MARKET` с мгновенным исполнением поведение остаётся прежним.

Здесь раньше стояло «алго-ордера не приходят стримом вообще». Это утверждение о
бирже, а проверено было только про деск: в документации USDⓈ-M на приватный
поток событие `ALGO_UPDATE` есть, и в нём есть `o.ai` — id порождённого ордера,
за которым это изменение ходит в REST. Приходит ли этот кадр на самом деле —
неизвестно, и как раз это меряет **шаг 36**. Если приходит, здесь и в
`futures-order-visibility` формулировку надо менять.

**Что изменение делает.** Пока сработавший родитель ещё числится в списке
открытых алго — а он там держится, пока порождённый ордер живёт, — деск больше не
рисует его как рабочий ордер на уровне триггера. И когда порождённый ордер
наконец закрывается, родитель уходит с экрана по стриму (доли секунды) вместо
следующей сверки (до 30 с). То есть выигрыш на закрытии, а не на срабатывании.

Поэтому проверять надо `STOP`/`TAKE_PROFIT` с **лимитной** ценой: порождённый
лимитник постоит в стакане, и всё, что изменение делает, будет видно. Замерено:
медиана промежутка между чтениями счёта — 30,0 с (n=127); плечо стрима на вашем
прокси — 345 мс (p50, n=200).

1. **Как биржа называет ваш стоп. Ничем не рискуя.** Это замер, а не проверка, и
   он решает, есть ли смысл в пунктах 2–4. Ордер ставить не надо: возьмите любой
   алго-ордер, который у вас уже висит, — а если ни одного нет, поставьте `STOP` с
   триггером далеко от рынка (сработать он не должен) и отмените сразу после
   замера. Снимите то, что биржа отдаёт по списку алго, и продиктуйте:

   - `algoType` — деск сейчас узнаёт только `CONDITIONAL`. **Если значение другое
     — дальше не идите:** изменение на ваших стопах не работает вообще, пункты
     2–4 проверять нечего, и чинить надо белый список, а не UI. Скажите значение.
   - `algoStatus` — какой он у ещё не сработавшего ордера. Если у сработавшего
     окажется `FILLED` или `FINISHED`, деск считает такой ордер закрытым и убирает
     из списка — тогда пункту 2 тоже нечего показывать.
   - `actualOrderId` — у несработавшего должен быть пустой строкой.

   Проще всего снять их из DevTools деска (вкладка Network, ответ
   `openAlgoOrders`) или из журнала биржи в браузере.

   Дальше — только если `algoType` оказался `CONDITIONAL`.

2. **Стоп, который сработает.** Поставьте `STOP` с **лимитной** ценой заметно
   ниже триггера (для SELL), чтобы порождённый лимитник постоял в стакане, и
   дайте цене пройти триггер. Пока лимитник стоит в стакане, смотрите на график и
   на строку в доке.

   Ожидается: метка на графике — пунктирная, бледнее, вместо суммы в USDT
   написано `triggered`; строка в доке говорит `fired`, в рейле — `FIRED`. Цена —
   та, по которой ордер реально прошёл (`actualPrice`), а уровень триггера
   остаётся под наведением мыши. В стакане уровень родителя больше не помечен как
   «мой ордер здесь».

   Если ничего этого нет — сверьтесь с тем, что записали в пункте 1: скорее всего
   дело в `algoType` или в `algoStatus`, и это нужно чинить, а не UI.

3. **Что деск на этом предлагает сделать.** Наведитесь и попробуйте кликнуть по
   строке сработавшего родителя и потянуть его метку.

   Ожидается: редактор не открывается, метка не тянется, кнопки Cancel нет, а
   подсказка объясняет почему — ордер уже не рабочий. Если что-то из этого
   предлагается, скажите: это ровно та ошибка, из-за которой изменение делалось.

4. **Уход с экрана на исполнении.** Дайте лимитнику исполниться (или отмените его
   на бирже) и засеките по секундомеру, через сколько родитель пропадёт из списка.

   Ожидается: доли секунды, а не до тридцати. Это единственный выигрыш во времени,
   который изменение даёт, и единственное место, где его видно.

5. **TWAP или VP.** Если у вас на бирже есть запущенный TWAP или VP — скажите:
   деск теперь намеренно не трогает такие алго (у них `actualOrderId` называет
   текущего ребёнка, и деск раньше убрал бы работающий алго с экрана насовсем).
   Проверить стоит, что такой алго по-прежнему виден в списке и не исчезает, когда
   его очередной кусок исполняется.

Плата: одно чтение счёта (вес 90) на каждое срабатывание алго. Тридцатисекундная
сверка остаётся как страховка и ничего не теряет.

*Закрывает:* `name-the-algo-order-that-fired` 4.3.

## Шаг 34. Что деск записал за прогон

Ордеров не требует. Делается последним — чтобы в журнале уже было что читать.

```
node scripts/read-desk-record.mjs
```

1. **Сколько на самом деле занимает ордер.** Ожидается: раздел «How long commands
   took to answer» с медианой и худшим временем по каждому виду команды. До
   правки в логе стояло только время отправки, поэтому «деск тормозит» нельзя было
   отличить от «биржа тормозит». Сравните медиану `trade.placeOrder` с тем, что
   видели глазами: если экран отстаёт от этой цифры, оставшееся уже не в бирже.

2. **Сколько раз деск ходил в REST и зачем.** Ожидается: раздел «Why the account
   was read» с причинами. `bootstrap` — по одному на запуск, `stream` — на каждый
   коннект/реконнект, `refresh` — ваши обновления и тридцатисекундная сверка,
   `unstated` — доборы за кадрами (по одному на серию, вес 5–10),
   `unresolved`/`command`/`setting` — редкие. Если `unstated` сопоставим по числу
   с количеством кадров, склейка не работает — скажите.

3. **Чтения на срабатывание алго.** Ожидается: на одно срабатывание — одно
   дополнительное чтение с причиной `refresh`, даже если филов по порождённому
   ордеру было несколько. Если чтений столько же, сколько филов, дедупликация не
   работает — скажите.

*Закрывает:* `keep-trading-while-the-account-is-read` 4.3,
`let-the-stream-state-the-account` 4.4, и последнюю часть
`name-the-algo-order-that-fired` 4.3 — сколько чтений стоило срабатывание.

## Шаг 35. Приватный поток действительно доносит

Стоит после шага 34 по номеру, но не по времени: шагу нужен перезапуск деска, и
он ни от чего в прогоне не зависит. Делайте его когда удобно — а лучше **до
части 3**, потому что он проверяет ровно то, от чего зависит, увидите ли вы фил
сразу или через полминуты.

Один лимитный ордер далеко от рынка, поставить и снять. Реальными деньгами не
рискует.

1. Перезапустите деск. В терминале должна появиться строка вида
   `[futures-stream] connecting wss://fstream.binance.com/private/ws?listenKey=<redacted>`.
   Она называет маршрут, на который деск подключился; ключ вырезан намеренно —
   строку можно пересылать.
2. Поставьте один лимитный ордер и снимите его.
3. Прочитайте журнал:

```
grep '"reason":"unstated"' ~/.config/cc-trade/diagnostics/desk-$(date +%F)-000.jsonl
```

Ожидается: **хотя бы одна строка с `"resources":1,"weight":5`.** Это и есть весь
признак. Такое чтение заводит только пришедший по потоку отчёт об исполнении, и
ничто другое в деске его выдать не может.

**«Поток подключился» признаком не считается.** До правки сокет открывался по
одиннадцать раз за день на адрес, который не присылал ни одного кадра, и в
журнале это выглядело здоровым.

Если строки нет — не чините сами, скажите. Ветвление такое: строки нет →
пробуем вторую форму адреса (`/private/ws/<listenKey>` вместо
`?listenKey=`); и её нет → дело не в адресе, а в listen key или прокси.

*Закрывает:* `prove-the-private-stream-is-carrying` 1.5.

**PASS (2026-08-13, 16:06 UTC).** Сессия поднялась в 16:06:19, сокет открылся в
16:06:24. Четыре команды — постановка, снятие, постановка, снятие (перенос ордера
деск делает как снятие и постановку заново) — и **четыре свёртки, ни одной
пропущенной**: `unstated` в :34.677, :37.183, :38.433, :41.183, все по
`resources: 1, weight: 5`. До этого в файле было 489 чтений и ни одного такого.

Кадр приходит вместе с ответом биржи, а не после него: склейка держит 400 мс, а
разрывы «ответ → чтение» вышли 390, 407, 364 и 343 мс, то есть отчёт об
исполнении был у деска за десятки миллисекунд до или после ответа на его
собственный HTTP-запрос.

Чего этот прогон **не** проверил: ни один ордер не исполнился, поэтому
`ACCOUNT_UPDATE` на фил — то самое, из-за чего ордер уходил с графика через
8–12 секунд — едет по уже доказанно работающему сокету, но глазами не виден.
Это проверяет шаг 30.

---

## Шаг 36. Биржа присылает `ALGO_UPDATE` — или не присылает

Из `hear-the-exchange-out`, §5.3. Это **замер, а не проверка**: от его ответа
зависит, останется ли лишнее чтение на 40 весов после каждой алго-команды.

*Риск: один `STOP`/`TAKE_PROFIT` с триггером далеко от рынка, отменяемый сразу.
Сработать он не должен.*

**Почему спрашиваем.** Официальная страница USDⓈ-M user data streams перечисляет
десять событий, и `ALGO_UPDATE` среди них — «пушится, когда создан новый
алго-ордер или изменился его статус». Деск исходил из обратного: шаг 33 выше
прямо говорит «алго-ордера не приходят стримом вообще», и на этом основании после
каждой алго-команды делается отдельное чтение списка алго. Документация — не
доказательство доставки: снятый маршрут четыре месяца отвечал на handshake и
молчал. Поэтому смотрим на провод.

**Что деск уже умеет.** Кадр `ALGO_UPDATE`, если он придёт, будет разобран и
свёрнут в список алго. Тридцатисекундная сверка и лишнее чтение оставлены на
месте намеренно — до вашего ответа ничего не убирается.

1. Поставьте `STOP` с триггером далеко от рынка.
2. Сразу же посмотрите в журнал деска (`~/.config/cc-trade/diagnostics`) и в лог
   приложения: пришла ли строка `[futures-stream]` про алго, и появился ли ордер
   в списке раньше, чем через 30 секунд.
3. Отмените ордер и посмотрите ещё раз: ушёл он с экрана сразу или на следующей
   сверке.
4. Продиктуйте: **пришёл / не пришёл**, и через сколько.

Если пришёл — предпосылка шага 33 неверна, и снятие лишнего чтения оформляется
отдельным изменением (`hear-the-exchange-out` §5.4). Если не пришёл — всё
остаётся как есть, и в записи это надо назвать прямо: «событие задокументировано,
на этом аккаунте не доставляется».

Дата, результат:

## Шаг 37. Плечо, изменённое в приложении Binance, доходит без чтения

Из `hear-the-exchange-out`, §5.3.

*Риск: нулевой. Меняется только настройка плеча на контракте, где нет позиции.*

**Почему спрашиваем.** Биржа объявляет смену плеча событием
`ACCOUNT_CONFIG_UPDATE`. Деск раньше этого не слушал и держал прочитанное
значение, пока не истечёт срок хранения, — то есть мог показывать плечо, на
котором счёт уже не стоит.

1. Откройте на деске контракт, по которому нет позиции, и дождитесь, чтобы плечо
   на нём отобразилось (значит, конфигурация прочитана).
2. В приложении Binance смените плечо на этом контракте.
3. Смотрите на деск, **не нажимая обновление**. Новое плечо должно встать в
   течение секунды, и промежуточного «—» быть не должно.
4. Продиктуйте, за сколько встало.

**Отдельно — то, чего не будет.** Смените там же режим маржи (кросс/изолированная)
на контракте, где нет позиции. Деск этого **не** увидит: в кадре биржи режима
маржи нет вовсе. Это не дефект, это предел того, что биржа сообщает. Проверьте и
запишите как ожидаемое: режим меняется только после чтения.

Дата, результат:

## Шаг 38. Молчащий поток рынка деск теперь замечает сам

Из `hear-the-exchange-out`, §5.3. Проверяется тем же приёмом, что и шаг 20:
остановкой прокси на `127.0.0.1:1080`.

*Риск: нулевой, если в этот момент нет открытых ордеров.*

**Почему спрашиваем.** До этого изменения восстановление начиналось только от
закрытия сокета. Сокет, который остался открыт и перестал приносить кадры,
помечался устаревшим и висел так сколько угодно. Теперь у каждого сокета свой
сторож: 15 с без кадра на потоке с гарантированным тактом, 400 с без кадра и без
ping'а на любом, 30 с молчания стакана при печатающей ленте.

1. Остановите прокси и засеките. Деск должен сам сказать, что поток не несёт, и
   в причине должен стоять **именующий себя** код: `STREAM_SILENT_15S`,
   `STREAM_INACTIVE_400S` или `BOOK_SILENT_THROUGH_TRADES_30S` — а не голый
   `SOCKET_CLOSED`.
2. Продиктуйте, какой именно код показан и через сколько секунд он появился.
3. Верните прокси. Деск должен вернуться сам, без перезагрузки окна и без
   переключения контракта.

**Что здесь важнее самого восстановления.** Остановка прокси рвёт соединение, и
его, скорее всего, поймает обычный путь по закрытию. Тогда в причине будет
`SOCKET_CLOSED`, и это правильно. Сторожа проверяет случай, который руками не
устроить: маршрут отвечает, соединение живо, кадров нет. Если вы увидели
`SOCKET_CLOSED` — так и запишите, а сторожа считайте закрытым только тестами.

Дата, результат:

## Шаг 39. Возврат к контракту, который деск уже держит

Из `keep-the-contracts-warm`, §5.4. Живая биржа, ордеров не нужно.

*Риск: нулевой. Ничего не отправляется.*

**Почему спрашиваем.** Деск держал ровно тот контракт, который показывал, и
каждое переключение платило полную сборку: каталог, свечи, тикер, премиум,
снимок стакана и три новых сокета. Теперь он держит до восьми контрактов сразу и
показывает один. Возврат к уже удерживаемому не читает у биржи ничего.

1. Откройте контракт **A** (например BTCUSDT) и дождитесь живого деска.
2. Переключитесь на **B**, потом на **C** — оба новые, они собираются как раньше.
   Продиктуйте на глаз, сколько занимает такая первая сборка.
3. Вернитесь на **A**. Здесь и смотрим. Должно прийти сразу и целиком: график,
   последняя цена, mark, тикер, **лента и стакан**. Стакан — главное: раньше он
   появлялся последним.
4. Пройдите A → B → A → C → A подряд, не быстрее чем успеваете смотреть. Ни на
   одном возврате не должно быть ни пустого графика, ни мигания между
   контрактами, ни пустого стакана.
5. Проверьте, что цифры на возврате **не отстали**: последняя цена на A должна
   совпадать с той, что показывает биржа сейчас, а не с той, что была, когда вы
   с A уходили. Контракт всё это время шёл; если цифра застряла — это дефект, и
   он важнее скорости.
6. Оставьте один контракт открытым минут на пять и вернитесь к нему. То же
   самое: свежие цифры, не замороженные.
7. Возьмите **девять** разных контрактов подряд, потом вернитесь к первому. Он
   уже вытеснен — граница восемь, — и должен собраться как новый, без ошибки и
   без пустого экрана.
8. Из `buy-the-page-only-for-the-book-on-screen`. После всего этого посмотрите
   журнал (`node scripts/read-desk-record.mjs`) и продиктуйте, сколько там строк
   `DEPTH_RANGE_SHORT`. До этой правки их было 155 за 14 часов на **одном**
   контракте, и восемь контрактов дали бы восьмикратно. Теперь их должно быть
   примерно столько же, сколько было при одном: страницу стакана деск покупает
   только тому контракту, который показывает. Если число выросло в разы — это
   дефект, и он бьёт по бюджету чтений того контракта, на котором вы торгуете.
9. И проверка обратной стороны той же правки: вернувшись к контракту, который
   долго стоял без показа, посмотрите на бейдж стакана. Он имеет право сказать
   «устарел» на один круг, пока докупается страница. Чего он не имеет права —
   быть зелёным над лестницей, из-под которой рынок уже ушёл.

**Что считать провалом.** Возврат, который проходит через «загрузка». Стакан,
который приходит позже остального. Цифра, застрявшая на моменте ухода. Экран,
который сам собой переехал на контракт, который вы не выбирали.

**Если захотите другую границу:** `CC_TRADE_FUTURES_HELD_CONTRACTS=4 npm run e`.
Допустимо 1..32; значение вне диапазона деск отвергает при старте, а не молча
подменяет.

Дата, результат:

## Шаг 40. Три ордера подряд, не дожидаясь первого

Из `lift-the-next-order-while-the-last-lands`, §4.4.

*Риск: два лимитных ордера минимального размера далеко от рынка. Оба отменяются
в конце шага.*

**Что было.** Запретов оказалось четыре, и каждая правка снимала только самый
внутренний из оставшихся; вы сообщали об одном и том же симптоме четырежды. А
когда все четыре сняли, вскрылся пятый дефект — уже не запрет, а недостача в
учёте, и он терял ордера с книги.

1. В хуке перетаскивания: пока деск должен был вернуть на книгу первый ордер,
   любой второй захват **молча** ничего не делал — `lift` возвращал `{ok:false}`,
   алерт `null`, на биржу не уходило ничего.
2. В графике: перетаскивание занимало единственный слот до ответа биржи, поэтому
   второе нажатие не доходило даже до хука — `onOrderLift` вызывался один раз
   вместо двух.
3. В главном процессе: очередь команд была одна на контракт, а перемещение ордера
   это отмена плюс постановка. Отмена, с которой начинается *следующее*
   перемещение, ждала позади постановки, заканчивающей предыдущее. Замер до
   правки: при летящей постановке отмена **другого** ордера на том же контракте
   не уходит вообще. Теперь очередь по ордеру; две команды об одном ордере
   по-прежнему строго по очереди.
4. **В жесте** — самый простой и последний. График держал указатель, пока биржа
   не ответит на отмену. А вы отпускаете кнопку задолго до ответа: круг через
   прокси 340–800 мс. Пока он идёт, следующий ордер **не схватить** — именно так
   вы это и описали. Все прежние замеры давали отмене ответить до отпускания
   кнопки, то есть проверяли путь, которым вы не ходите; поэтому запрет и
   пережил три правки.

5. **В учёте — и это стоило вам ордеров на живой книге.** Когда все четыре
   запрета сняли, перетаскивать сразу несколько стало можно, и деск начал
   **терять ордера**: вы двигали три — оставалось два, двигали два — оставался
   один. Обязательство «деск должен вернуть ордер на книгу» хранилось в **одной
   ячейке**, а команда «опустить» не знала, какой ордер она опускает: брала то,
   что было поднято последним. Пока жест не мог начаться до посадки предыдущего,
   ячейка всегда успевала освободиться. Как только смогли — второй подъём затирал
   первый, и счёт сходился с недостачей ровно в один ордер на каждый лишний
   одновременный жест.

   Хуже, чем просто пропажа: **уцелевший ордер вставал чужим размером по чужой
   цене**. Замер до правки, дословно: единственная постановка вместо двух была
   `{"price":"58500","quantity":"0.007"}` — объём второго ордера по адресу
   первого. Плашка при этом не поднималась: деск считал, что всё сделал.

   График имя ордера передавал всегда — его теряло промежуточное звено. Тесты с
   обеих сторон шва были зелёные всё это время, поэтому теперь есть тест на самом
   шве.

   Там же вылезла вторая дорога к потере: мышь сообщает **один и тот же**
   `pointerId` на каждый жест, и завершившееся перетаскивание отдавало указатель
   обратно — забирая его у того, которое вы держите прямо сейчас.

Теперь указатель освобождается в момент, когда вы отпустили кнопку, и остаётся у
того жеста, который вы делаете. Ордер, чья отмена ещё летит, остаётся
нарисованным и встаёт на то место, где вы его оставили, когда биржа ответит.

1. Поставьте **три** лимитных ордера на одном контракте, все далеко от рынка, и
   **разного размера** — так подмену объёма видно сразу. Запишите три пары
   «цена — объём» до начала.
2. Перетащите первый и **сразу**, не дожидаясь, пока он встанет на новое место,
   хватайте второй, затем так же третий.
3. Сверьте книгу по счёту и по объёмам: должно остаться ровно **три** ордера, и
   у каждого — **свой** объём на новом месте. Продиктуйте: сколько осталось, и
   совпали ли объёмы с записанными.
4. Повторите на **двух разных** контрактах.
5. Отмените всё кнопкой **`Cancel all <СИМВОЛ>`** — она внизу вкладки ордеров в
   правой панели и появляется только когда на выбранном контракте есть ордера.
   Проверьте на телефоне или в браузере Binance, что не осталось ни одного.
   Это отдельная проверка: `Cancel all` теперь единственная команда, которая
   по-прежнему держит весь контракт, и её никакая постановка обогнать не должна.

**Что должно быть видно, если что-то пойдёт не так.** Каждый неудавшийся возврат
теперь заявляется отдельной плашкой со своим контрактом и своей кнопкой «поставить
снова». Если увидите две плашки — это не дефект, это и есть новое поведение;
проверьте, что кнопка на одной не трогает вторую.

Дата, результат:

## Шаг 41. Размер частично исполненного ордера

Из `price-an-order-at-what-still-rests`, §2.4.

*Риск: один лимитный ордер, который вы дадите исполнить наполовину. Остаток
отменяется в конце шага.*

**Что было.** Вы сказали: «ордер исчезает, но сумма не меняется, если он
исполнился например на 50%». Исполненный целиком уходил с графика сам — то есть
поток исполнения деск слышал. А число не менялось, потому что **все** поверхности
оценивали ордер по размеру, с которым он был поставлен, а не по остатку.
Исполненная половина при этом уже показана как позиция — деск считал её дважды и
завышал ваше рабочее плечо ровно на исполненную часть.

Замер до правки, дословно: `Expected: "500"`, `Received: "1000"` — ордер на 10 по
100 с пятью исполненными.

Перетаскивание всё это время знало правильный ответ: подняв наполовину
исполненный ордер и опустив, вы ставили **остаток**, а не то число, которое деск
показывал. Два чтения расходились, и неверным было именно то, что на экране.

1. Поставьте лимитный ордер вплотную к рынку так, чтобы он исполнился частично —
   удобнее на активной монете. Запишите размер до исполнения.
2. Дождитесь частичного исполнения. Сверьте **четыре** места: подпись на графике,
   строку в правой панели, колонку «Size (USDT)» в доке и общую сумму в тикете.
   Все четыре должны показывать **остаток**, а не исходный размер, и совпадать
   между собой.
3. В доке колонка исполненного объёма должна показывать исполненную часть —
   она отвечает на другой вопрос и меняться не должна.
4. Наведите курсор на размер: точное число контрактов тоже должно быть остатком.
5. Отмените остаток и сверьте на Binance, что ничего не осталось.

Дата, результат:

## Шаг 42. Стакан держится под рынком

Из `keep-the-book-under-the-market`, §3.3.

*Риска нет: ничего не отправляется, только чтение.*

**Что было.** Вы сказали: «стаканы не успевали заполняться — когда цена скакала,
то был перекос: то одна сторона только видна, потом только вторая». Это не
отрисовка. Снимок книги доказывает полосу цен; уровень за её краем деск
выбрасывает намеренно — соседи такого уровня не читались, и строка, склеенная
через дырки, занизила бы рынок. Пока рынок уходит вверх, сторона продаж
перестаёт получать уровни и пустеет, сторона покупок остаётся полной.

Ответ на это — перечитать ту же страницу там, где рынок сейчас. Деск умел
приходить к этому перечитыванию одним путём: когда страница дотягивалась до
строк на экране, но рынок из неё ушёл. Если страница до строк **не** дотягивалась,
деск покупал страницу глубже — а на самой глубокой покупать нечего, и он выходил,
не прочитав ничего. И это состояние не проходит само: то, что доказала страница,
зафиксировано моментом чтения.

Замер по бирже 2026-08-13 через ваш же прокси, тысяча уровней на сторону:

| Контракт | вниз | вверх |
|---|---|---|
| AKEUSDT | 4.10% | 3.90% |
| BTCUSDT | 0.19% | 0.19% |
| ETHUSDT | 0.55% | 0.55% |
| TUTUSDT | 33.22% | 28.35% |

Самый грубый шаг группировки просит на AKEUSDT 9.1% цены при четырнадцати
строках — против опубликованных 4.1%. То есть на AKE на вашем шаге деск попадал
в эту ветку с первого кадра и оставался в ней весь сеанс: полоса больше не
двигалась, и каждый пробой уносил сторону книги.

В журнале за 2026-08-13 — 153 отказа `DEPTH_RANGE_SHORT`, из них 60 между 18:00 и
20:00 UTC, когда вы торговали.

1. Откройте AKEUSDT (или другую монету, которая в этот момент резко ходит) и
   поставьте самый грубый шаг группировки — тот же, на котором был перекос.
2. Дождитесь резкого движения. **Обе стороны должны продолжать заполняться.**
   Сторона, в которую ушёл рынок, может на секунду поредеть, но не должна
   оставаться пустой, пока вторая полна.
3. Оставьте контракт открытым на спокойном рынке минут на десять и посмотрите
   `~/.config/cc-trade/diagnostics/desk-<дата>-000.jsonl`: `DEPTH_RANGE_SHORT` на
   стоящем рынке появляться не должен — перечитывание нужно только когда рынок
   уходит.
4. Сравните число `DEPTH_RANGE_SHORT` за сеанс с 153 за 2026-08-13.

Дата, результат:

## Шаг 43. Лестница шагов кончается там, где кончается книга у деска

Из `end-the-book-where-the-market-does`, §4.3.

*Риска нет: ничего не отправляется, только чтение.*

**Сначала — где я ошибся.** Я написал вам, что книги на ±75% цены не существует,
и что тысяча уровней на сторону — это всё, что публикует Binance. Вы спросили,
как тогда в приложении Binance видно больше 100%. Вы правы, я — нет.

Тысяча уровней — это одна REST-страница. **Поток `@depth` ею не ограничен.**
Замер на AKEUSDT 2026-08-13 через ваш прокси: за 60 секунд поток принёс 8196
уровней за пределами полосы снимка, 5645 из них — живые заявки, дальше всего
−87.55% и +217.86% от цены. Приложение Binance рисует дальнюю книгу, потому что
не выбрасывает то, что приносит поток. Деск выбрасывает — намеренно, но, как
теперь видно, ценой почти всей книги.

Подержав поток три минуты, книга оказывается маленькой и очень длинной. Слово
«целиком» здесь неверно, и это выяснилось позже: за десять минут она вырастает
вдвое и продолжает расти — см. таблицу в шаге 44. Три минуты — просто ранний
срез, а «докуда достаёт» в этой таблице — до самого дальнего уровня, то есть до
чьего-то одинокого лимитника, а не туда, где у книги есть вес:

| Сторона | уровней всего | докуда достаёт | сколько USDT лежит в ближней тысяче |
|---|---|---|---|
| AKEUSDT покупки | 2431 | 83.96% | 17.7% |
| AKEUSDT продажи | 3183 | 1078.75% | 7.1% |
| BTCUSDT покупки | 3397 | 98.41% | 12.3% |
| BTCUSDT продажи | 3425 | 66.67% | 16.2% |

Деск держал ближнюю тысячу — то есть **выбрасывал от 82% до 93% стоящих заявок
по деньгам**. Это и был ваш «максимум 10% цены»: не потолок биржи, а наш. Дальнюю
книгу деск теперь держит, и лестница выросла вместе с ней: к ступеням добавились
2000, 5000, 10000 и 20000 тиков, ни одна прежняя не сдвинулась.

**Что здесь изменилось с тех пор, как шаг был написан — и что вы должны увидеть
вместо обещанного тогда.** Лестница кончается там, где у книги **есть вес**, а не
где лежит самый дальний уровень. Замер 2026-08-14, пять минут потока: самый
дальний аск на AKEUSDT стоял на **1 357 378%** цены, а сотая доля стороны за ним —
на 145%; на BTCUSDT самый дальний бид на 99.0% против 10.05%. Половина бидов
AKEUSDT лежит внутри 11% цены, половина бидов BTCUSDT — внутри 0.8%.

Отрезав по самому дальнему уровню, деск предлагал бы вам шаг, чьи четырнадцать
строк накрывают рынок, которого там нет — то есть **та же пустая панель, только
заходящая с другого конца**. Поэтому чтение отбрасывает дальнюю сотую долю каждой
стороны, и подпись `±…%` рядом с выбором шага означает «докуда у книги есть
уровни», а не «где лежит чей-то одинокий лимитник».

Проверяется именно это: **список шагов и подпись рядом с ним сходятся с тем, что
нарисовано.**

1. Откройте AKEUSDT, разверните список шагов до конца. Последний шаг должен
   заполнять **все** строки стакана, без пустых снизу или сверху. Это главный
   пункт: если внизу или вверху пусто — чтение врёт, и это дефект.
2. Рядом со списком шагов должно стоять число вида `±30%`. Наведите курсор —
   в подсказке точные расстояния по обеим сторонам и оговорка, что дальняя сотая
   доля отброшена.
3. Сверьте: это число должно быть примерно там, докуда доходят строки стакана
   относительно последней цены. Не точно до знака — оно про книгу целиком, а
   строки обрезаны выбранным шагом.
4. Повторите на контракте с совсем другой ценой. Список шагов должен быть
   **разной длины** на разных контрактах: он про книгу, а не про тик. На
   большинстве контрактов он и раньше кончался раньше конца лестницы — новые
   ступени видны только там, где цена мелко нарезана тиком (BTC, ETH, AKE, BNB).
5. Если вы раньше выбирали на контракте шаг, которого теперь в списке нет —
   стакан нарисуется на самом грубом доступном, а ваш выбор останется
   запомненным и вернётся, когда книга станет глубже.

Дата, результат:

## Шаг 44. Стакан достаёт туда же, куда приложение Binance

Из `keep-the-book-the-stream-restates`, §5.4.

*Риска нет: ничего не отправляется, только чтение.*

**Что было.** Вы спросили, как в приложении Binance видно больше 100% цены.
Ответ: оно не выбрасывает дальнюю книгу, а деск выбрасывал. Уровень за пределами
полосы, которую доказал REST-снимок, деск отказывался применять, и держал только
ближнюю тысячу уровней на сторону.

Замер 2026-08-13: за 60 секунд поток `@depth` на AKEUSDT принёс 8196 уровней за
полосой, 5645 из них живые, дальше всего −87.55% и +217.86%. В ближней тысяче,
которую держал деск, лежит **7–18%** стоящих денег. Остальное деск выбрасывал
десять раз в секунду.

**Сколько уровней «вся книга» — числа не существует, и это важно для пункта 4.**
Замер 2026-08-14, уровней на сторону:

| | 1 мин | 3 мин | 5 мин | 10 мин |
|---|---|---|---|---|
| AKEUSDT | 1658 / 1801 | 2403 / 2420 | 4017 / 3427 | **6197 / 4665** |
| BTCUSDT | 1580 / 1555 | 2689 / 2615 | 4157 / 4122 | **6270 / 5185** |

На десятой минуте всё ещё растёт. Деск держит **10 000 уровней на сторону**,
дальше выселяет самые дальние. Это накрывает обе измеренные книги с запасом, так
что за десять минут наблюдения вы в потолок упереться не должны — если книга
дорастёт до него на очень долгой сессии, дальность выйдет на полку, и это будет
ограничение деска, а не рынка.

Потолок был 4000 и поднят вместе с тем, что за него платили: разбор десятичной
строки платился заново за каждый уровень на каждом кадре. После правки 10 000
уровней стоят меньше, чем вчера стоили 4000 — на упёршейся книге кадр 4.8 мс
против 7.9.

Теперь уровень применяется там, где он стоит, а полоса снимка означает то, что
всегда означала: докуда книга сходится **полностью**. Дальше неё книга знает то,
что поток успел назвать — по каждому названному уровню точно, и молчит о тех,
кого никто не трогал. Строки за этой границей помечены.

1. Откройте один и тот же контракт в деске и в приложении Binance, поставьте
   сопоставимый шаг группировки. Сравните дальние строки: размеры должны
   сходиться, а не быть пустыми у нас.
2. На AKEUSDT (или другой дешёвой монете) уйдите на самый грубый шаг. Строки
   должны быть заполнены **до конца панели**, а не обрываться через шесть.
3. Найдите границу «дочитанной» книги — строки за ней помечены. Убедитесь, что
   пометка стоит там, где книга перестаёт быть полной, а не на первой строке и
   не на всех подряд.
4. Подержите контракт открытым десять минут и посмотрите, выросла ли дальность
   (подпись `±…%` рядом с выбором шага). Она должна расти **все десять минут** —
   потолок удержания их накрывает. Дефектом будет, если число не растёт с самого
   начала, падает, или встаёт колом задолго до десятой минуты.
5. Поставьте дальний ордер на пробой и проверьте, что он виден на своей строке
   стакана, а не только на графике.

Дата, результат:

## Шаг 45. Правка ордера видна сразу, без перезагрузки

*Риска нет для самой проверки, но она делается на живом ордере — ставьте
минимальный.*

**Что было.** Вы сообщали: поменяли объём ордера, биржа приняла, панель
закрылась без ошибки, а деск не показал новый объём, пока вы не нажали Ctrl+R.

**Что я нашёл, и почему прошу вас проверить, а не докладываю о починке.**

Я прошёл весь путь: подтверждение правки → PUT на биржу → ответ → применение к
списку ордеров → отрисовка. Дефекта в нём не видно, и вот что мешает мне
объявить его живым:

1. **Сторож для ровно этого симптома стоит с 10 августа.** Снимок ордеров у
   Binance — отдельная служба, и сразу после правки она ещё может отдавать
   старый объём. Деск сравнивает отметку времени биржи на своём ордере и на
   пришедшем снимке и оставляет более новый. Это покрыто тестом.
2. **Документация Binance проверена, а не вспомнена.** Ответ на правку несёт
   тот же `orderId` и `updateTime` — то есть деску приходит всё, что нужно,
   чтобы применить правку у себя.
3. **В журнале деска семь правок с 12 августа** (APR, TUT, AKE) — все с ответом
   `ok`. Ни одна не повторялась на том же ордере, а повтор — это первое, что
   сделал бы оператор, не увидевший результата.

Значит либо дефект уже закрыт правкой от 10 августа и ваш случай был до неё,
либо он в состоянии, которого я не воспроизвёл. **Самый вероятный кандидат на
второе — частично исполненный ордер:** там объём меняется с двух сторон
одновременно, и мой прежний вопрос к вам про это так и остался без ответа.

1. Поставьте лимитный ордер минимального размера подальше от цены.
2. Поменяйте объём. Панель должна показать новый объём **сразу**, до всякого
   обновления.
3. Поменяйте цену тем же способом. Линия на графике должна переехать сразу.
4. Если объём не поменялся — **не жмите Ctrl+R**, подождите тридцать секунд:
   деск переспрашивает счёт на медленном такте, пока есть рабочие ордера. Если
   после этого объём стал верным, дефект в применении ответа; если остался
   старым — в самом чтении. Разница решает, где чинить, а Ctrl+R её стирает.
5. Если поймаете — скажите, был ли ордер частично исполнен к моменту правки.

Дата, результат:

## Шаг 46. Док на узком окне и клавиатура вместо мыши

Из `reach-the-desk-without-a-mouse`, §3.2.

*Риска нет: ничего не отправляется, только чтение и один Escape.*

**Что было.** На узком окне сетка деска не имела места для дока: он выпадал в
безымянную колонку шириной 78 px и становился нечитаемым. Отдельно от этого
строку ордера можно было открыть только мышью.

Измерено в Chromium на двенадцати ширинах от 320 до 1280 px: узкая раскладка
идёт до 844 px, компактная настольная с 845 до 984, обычная с 985. На каждой
ширине док на месте, внутри сетки, не пересекает панели, и до его таблиц можно
доскроллить.

1. Сузьте окно деска примерно до половины экрана, потом ещё уже, насколько
   позволяет система. Док (позиции, ордера, `CLOSED POSITIONS`) должен
   **оставаться на месте** и читаться — своей строкой, а не обрезанным столбцом
   сбоку.
2. На каждой ширине проверьте, что до таблиц дока можно добраться: вертикально
   до самого дока, горизонтально — до правых колонок.
3. Верните обычную ширину. Пройдите **клавиатурой**: Tab до строки рабочего
   ордера — на ней должна быть видимая рамка фокуса.
4. Нажмите Enter (потом, отдельной попыткой, пробел). Должна открыться та же
   панель правки ордера, что и по клику. Escape закрывает.
5. То же на строке ордера в тикете справа.
6. Подержите Enter зажатым на строке: панель должна открыться **один раз**, а не
   столько раз, сколько сработал автоповтор.

Дата, результат:

## Шаг 47. Обзор ордеров отвечает «сделали ли мои ордера хоть что-нибудь»

Из `make-the-order-history-readable`, §5.3.

*Риска нет: только чтение, ни одной команды из этой панели не уходит.*

**Что было.** Обзор объявлял восемь колонок. На широком окне док делится
надвое, и дорожки `Status` и `Type` получали 62.7 px при том, что
`PARTIALLY_FILLED` требует 125, `EXPIRED_IN_MATCH` — 125, `STOP_MARKET` — 86.
Они обрезались в многоточие, и слова биржи не было даже в подсказке: восстановить
его было неоткуда. Ниже 1461 px док складывается в одну колонку и всё
помещалось — то есть ломалось именно то окно, в котором вы работаете.

Теперь колонок шесть, и первой идёт не колонка, а исход:
`Filled` / `Part 38%` / `Open` / `Expired` / `Rejected`. Слово биржи — под
курсором на самом чипе.

1. Откройте `Order history`. Первое, что читается в строке, — что стало с
   ордером, без горизонтального скролла и без многоточий.
2. Наведите курсор на чип исхода. Должно показаться собственное слово биржи
   (`FILLED`, `PARTIALLY_FILLED`, `EXPIRED_IN_MATCH`).
3. Найдите частично исполненный ордер: чип должен называть долю (`Part 38%`).
4. Ордер, который ничего не исполнил, должен быть **приглушён**, но остаться на
   месте и читаться.
5. Колонка `Type`: вместо `· RO` теперь бейдж `exit`. Под курсором — точный тип
   биржи и слова `reduce-only`.
6. Колонка `Price`: у рыночного ордера показывается `≈` и средняя цена
   исполнения. Под курсором — обе цены, названные словами.
7. Строки сгруппированы под заголовками дней (`TODAY`, `09.08`), и **в каждой
   строке стоит время**, а не дата.
8. Чипы над таблицей: `All` / `Filled` / `Unfilled` и `This contract`.
   Пощёлкайте их. Строка под таблицей («N contracts read, back to …») **не
   должна меняться**: она описывает чтение, а не то, что осталось после фильтра.
   Никакого перечитывания счёта фильтр вызывать не должен.
9. Проверьте на своей обычной ширине окна и растянув окно на весь экран.

Дата, результат:

## Шаг 48. Деск не показывает состояние, в котором счёта нет

Из `report-execution-state-truthfully`, §7.2.

*Риск: шаг требует уронить связь. Ордеров не ставьте, пока не вернёте её.*

**Что было.** Док знал о счёте две вещи — прочитано и не прочитано — и рисовал
один и тот же список для всего остального. Снимок, который деск держит и который
никто не подтверждал после обрыва, выглядел ровно как подтверждённый секунду
назад. Отдельно: отказ биржи считывался живьём, и отчёт об исполнении **чужого**
ордера стирал его с экрана.

1. При живой связи откройте док. Пока всё прочитано, никаких плашек о
   синхронизации быть не должно — только строки.
2. Остановите прокси `127.0.0.1:1080`. В доке над таблицами должно появиться
   `Not confirmed since the connection dropped — showing the last reading.` с
   причиной и кнопкой `Retry`. **Строки при этом остаются на месте** — их не
   должно убрать.
3. В тикете справа рядом с `Available` должен появиться возраст баланса
   (`1m 32s old`), и он должен **расти**. Ползунок размера и поле `Notional`
   должны быть заблокированы.
4. Верните прокси. Нажмите `Retry` в доке. Плашка и возраст должны уйти, когда
   счёт подтвердится.
5. Отдельно: поставьте заведомо отказной ордер (например, ниже минимального
   номинала). Карточка отказа с кодом Binance должна **остаться на экране** —
   даже если в это время исполнится какой-то другой ваш ордер. Уйти она должна
   только по кнопке `Dismiss` или когда вы отправите следующий ордер.
6. Отдельно: при остановленном прокси вызовите ордер жестом и нажмите `Send`.
   Панель подтверждения **не должна закрываться**: цена и размер остаются, и на
   самой панели написана причина.

Дата, результат:

# Результаты

Заполните и верните сессии.

| Шаг | Что | Результат |
|-----|-----|-----------|
| 1 | Пять ресурсов счёта готовы | PASS (2026-08-13) |
| 2 | График, стакан, лента живые | PASS (2026-08-13) |
| 3 | Переключения без шторма отказов | PASS — переключает быстро, терминал чист (2026-08-13) |
| 4 | Док говорит «не прочитано», а не «0» | PASS — но само состояние «ещё не прочитано» поймать не удалось: NOT OBSERVABLE (2026-08-13) |
| 5 | Обзор счёта читается один раз | PASS (2026-08-15) — «переключение мгновенное во всех вариантах» |
| 6 | Вёрстка | PASS (2026-08-15) — «всё отображается нормально» |
| 7 | 15 контрактов за минуту, бюджет цел | PASS (2026-08-15) — «всё грузится ОЧЕНЬ БЫСТРО, без лагов и багов, ничего не вылетает, стакан целый, трейды целые» |
| 8 | Стакан на всех шагах группировки | PASS (2026-08-15) — «всё ок», сверено с приложением Binance |
| 9 | Обе стороны книги под своим бейджем | PASS (2026-08-15) — «всё ок» |
| 10 | Тихий контракт: стакан с первого снимка | PASS (2026-08-15) — «всё отлично работает», сверено с приложением Binance |
| 11 | Тихий график: QUIET, а не запрет | п.1 PASS (2026-08-12); п.2–4 не проходились |
| 12 | Быстрый контракт: стакан и лента ровно | PASS (2026-08-15) — «стакан синхронен с trades, всё ок» |
| 13 | Спот: догрузка, глубина, RSI, месяц | PASS (2026-08-15) — догружает при каждом проматывании, ничего не съезжает, `1M` загрузился. Оператор отдельно: «работает всё гораздо медленней чем на фьючах», подозревает избыток REST там, где мог бы быть WS — заведено отдельно, см. ниже |
| 14 | Журнал деска читается и чист | PASS (2026-08-15) — прочитан сессией по просьбе оператора: 741 события, 0 ресинхронизаций, в записях нет ни ключа, ни подписи, ни цены, ни объёма, ни суммы, ни PnL. Две находки в сводке разобраны отдельно (перекрестье книги — исправлено `ef7223b`; свободная маржа 0/68 — передано владельцу) |
| 15 | Прокси остановлен | PASS (2026-08-15) — «получил кучу ошибок и информацию о том что связь прервана» |
| 16 | Замерзший mark не выдаётся за рынок | PASS (2026-08-13) |
| 17 | Отказ чтения счёта с причиной | PASS — «The Futures account request could not reach Binance…» / «LONG ENTRY NOT SENT» (2026-08-13) |
| 18 | Панель остаётся открытой | PASS (2026-08-15) — первая попытка: панель открылась, дала изменить сумму, отправка вернула `ECONNRESET`, панель осталась открытой. Вторая попытка: поля уже заблокированы. Журнал подтверждает обе: два `outcome` `trade.replaceOrder` `rejected`, `exchangeCode: ECONNREFUSED`, 18:01:25 и 18:03:08. Дефект «панель закрывается, будто получилось» не воспроизводится |
| 19 | Futures-график разблокируется, плашка красная | **FAIL (2026-08-15)** — «включил снова прокси — свечи не загрузились, начал двигать, скроллить — свечи не грузятся», строка `Older candles could not be loaded — scroll again to retry` остаётся висеть. Плашку `STALE` оператор не описал — эта половина не проверена. Причина найдена по коду: `readFailed` снимается ровно одним местом (`applyCandleHistoryPage`, приход страницы), поэтому строка переживает единственное, что её гасит, и обещанный ею повтор невыполним. Заведено отдельно |
| 20 | Рынок возвращается сам | PASS (2026-08-15) — п.1 закрыт: «биржа восстановилась самостоятельно, на 5–7 секунде начал тикать PnL, на 18–19 всё остальное восстановилось», без `Ctrl+R`; следом успешно отредактирован существующий ордер. п.2 и п.3 PASS (2026-08-13) |
| 21 | Спот-график разблокируется | PASS (2026-08-15) — «стакан восстановился практически сразу, через 10–15 с вернулись трейды; свечи подтянулись сразу, как только начал двигать график». На споте повтор работает — на фьючах (шаг 19) нет, и это та же кнопка на двух разных путях |
| 22 | Уходит подтверждённый размер | НЕ ПОНЯТ (2026-08-15) — оператор: «нужно более подробное объяснение, то что ты написал — непонятно». Шаг переписан, проверка не проводилась |
| 23 | Ордера чужих контрактов в списке | PASS (2026-08-15) — «с ордерами никогда проблем не было, всё ок» |
| 24 | Отказ несёт код биржи | PASS (2026-08-15) — на экране «Size is below the Binance minimum notional»; в журнале `outcome` `rejected` с `exchangeCode: "-4164"` рядом с `FUTURES_API_ERROR` (18:12:29), текст биржи в файл не попал. Побочно вскрыт отдельный дефект: перетаскивание ордера ниже минимума отменяет живой ордер и не ставит замену — журнал 18:12:27 `cancelOrder` ok → 18:12:29 `placeOrder` rejected, ордера нет |
| 25 | Ордер появляется один раз | PASS (2026-08-15) — «всё ок» |
| 26 | Серия ордеров не выключает торговлю | PASS (2026-08-15) — «поставил сразу 6 ордеров и отменил 6, визуально тормозов не увидел». Бейдж переходил `LIVE`→`SYNC` и возвращался, не залипал. Журнал 18:13–18:14: ни одного отказа, ни одного `NOT sent` |
| 27 | Драг идёт за рукой | PASS (2026-08-15) — «работает как надо» |
| 28 | Одна команда — одна отправка | NOT REPRODUCIBLE (2026-08-15) — оператор: «по шорткату сначала идёт подтверждение, а если быстро прокликать попап — он просто пропадает и никаких 2 ордеров не получается». Двойная отправка недостижима рукой, потому что между нажатием и отправкой стоит подтверждение |
| 29 | Cancel all убрал и стоп — проверено на Binance | ЧАСТИЧНО (2026-08-15) — «нажал Cancel all BTW, всё сработало, оба ордера пропали». Но за весь день не поставлено ни одного стоп-ордера (журнал: 47 `LIMIT`, 2 `MARKET`, 0 условных), а шаг существует ровно ради того, что стоп переживает Cancel all под опустевшим списком. Обычная половина проверена, ключевая — нет. Кнопку оператор нашёл: она по контракту, `Cancel all <КОНТРАКТ>` |
| 30 | Фил, позиция, PnL, плечо, закрытие | PASS (2026-08-15) — «всё работает». Оператор попросил позже отдельно посмотреть, как обновляется Total ордера в USDT, когда крупный ордер откупается по частям |
| 31 | Фил уходит сразу на быстром движении | ОТЛОЖЕН (2026-08-15) — «это уже надо по факту больших движений тестить» |
| 32 | Закрытая позиция остаётся закрытой | НЕ ПОНЯТ (2026-08-15) — «не понимаю что это значит; закрытые позиции всегда уходили в закрытые, ни разу не видел чтобы возвращались». Шаг переписан, проверка не проводилась |
| 33 | Сработавший ALGO не рабочий ордер | ОТЛОЖЕН (2026-08-15) — «ликвидацию тоже надо тестить отдельно» |
| 34 | Журнал: тайминги и причины чтений | |
| 35 | Приватный поток доносит: одно чтение вес 5 | PASS — 4 команды, 4 свёртки, 16:06 UTC (2026-08-13) |
| 36 | `ALGO_UPDATE` приходит или не приходит | ЗАМЕР НЕ СОСТОЯЛСЯ (2026-08-15) — оператор счёл, что данных уже достаточно («много ставил, двигал и отменял»), но за весь день не отправлено ни одной условной команды: журнал даёт `placeOrder` 49 (47 `LIMIT`, 2 `MARKET`), `cancelOrder` 44, `replaceOrder` 8 и ноль алго. В записи нет ни одного алго-события, потому что их нечем было вызвать. Лишнее чтение на 40 весов после каждой алго-команды остаётся неразрешённым |
| 37 | Плечо из приложения доходит без чтения | ОТЛОЖЕН (2026-08-15) — «тоже потом» |
| 38 | Молчащий поток рынка деск замечает сам | ШАГ НЕГОДЕН (2026-08-15) — оператор остановил прокси и получил «кучу ошибок и resyncronizing, ордера ставить не могу». Это обрыв связи, а не молчащий поток: остановкой прокси нужное состояние — открытый сокет, переставший приносить кадры — вызвать нельзя. Виноват шаг, а не деск; требует переписывания под другой способ вызова |
| 39 | Возврат к удерживаемому контракту мгновенный | PASS (2026-08-15) — «несколько переключений между AKE, APR, ACE в разных комбинациях, всё переключается seamlessly, даже не теряем стаканы» |
| 40 | Три ордера подряд, не дожидаясь первого | PASS (2026-08-15) — «схватил и поперетаскивал несколько ордеров подряд до того как они успели переставиться, всё нормально сработало» |
| 41 | Размер частично исполненного ордера | ОТЛОЖЕН (2026-08-15) — «тоже по факту движений надо тестить, сразу не могу» |
| 42 | Стакан держится под рынком на пробое | ОТЛОЖЕН (2026-08-15, подтверждён повторно) — «когда будут резкие пробои и движения — тогда и сообщу наблюдения». Рукой не вызывается |
| 43 | Лестница шагов сходится с книгой, которую держит деск | PASS (2026-08-15) — «сверил с binance app, всё ок» |
| 44 | Стакан достаёт туда же, куда приложение Binance | PASS (2026-08-15) — «всё ок» |
| 45 | Правка ордера видна сразу, без перезагрузки | PASS (2026-08-15) — «всё работает как надо». Закрывает дефект правки ордера, о котором оператор сообщал ранее |
| 46 | Док на узком окне и клавиатура вместо мыши | ОТКАЗ (2026-08-15) — оператор: «я НИКОГДА не буду торговать клавиатурой». Клавиатурная половина не будет проверена никогда; половина про док на узком окне измерена в Chromium на 12 ширинах |
| 47 | Обзор ордеров отвечает «сделали ли мои ордера хоть что-нибудь» | |
| 48 | Деск не показывает состояние, в котором счёта нет | |

**Ранее найденные дефекты, оба исправлены и ждут проверки в этом прогоне:**

1. После обрыва длиннее ~90 секунд график, стакан и лента не возвращались никогда
   — помогал только `Ctrl+R`. Счётная часть при этом оживала за ~8 секунд, поэтому
   деск выглядел здоровым, будучи слепым. Заведено как
   `recover-the-market-feed-after-an-outage`, проверяется шагом 20.
2. Приватный поток счёта не доносил ничего — он подключался на маршрут, снятый
   биржей 2026-04-23, и деск молча перечитывал весь счёт раз в 30 секунд по весу
   90. Заведено как `prove-the-private-stream-is-carrying`; маршрут исправлен,
   проверяется шагом 35, и шаг 35 уже пройден. Остальные разделы того изменения
   (поток заявляет, что он несёт, а не что он открыт) ещё в работе — до них
   «открыт» по-прежнему считается за «несёт».

Дата, аккаунт (Production), ревизия деска (`git rev-parse --short HEAD`):

---

# Чего этот прогон закрыть не может

## Клавиатурное открытие панели ордера

`reach-the-desk-without-a-mouse` 3.2, вторая половина. Оператор 2026-08-15,
дословно: «я НИКОГДА не буду торговать клавиатурой». Это не отказ проверять — это
заявление о том, как деском пользуются, и проверка, которую никто никогда не
выполнит, не станет правдивее оттого, что висит открытой.

Чем закрыто вместо этого: `FuturesPortfolioDock.test.jsx` и
`FuturesTradingTicket.test.jsx` — строка получает фокус, Enter и пробел открывают
ту же панель, что и клик, зажатая клавиша открывает её один раз. Против кода до
изменения эти тесты падали.

Первая половина той же задачи — док на узком окне — **измерена**, а не оставлена:
Chromium, двенадцать ширин от 320 до 1280 px, на каждой док внутри сетки, без
пересечений, до таблиц можно доскроллить.



Запишите как `COVERED BY TEST ONLY` — это реальные гарантии, но ситуацию нельзя
устроить руками.

## Неоднозначный исход команды

Касается `harden-trading-command-integrity` 7.2 и
`answer-the-command-that-asked` 4.2 (первая половина).

Обе задачи требуют, чтобы команда ушла на биржу и **ответа не пришло** — тогда
деск обязан сказать «не знаю», а не «отклонено», сверить ордер по клиентскому
идентификатору и не создать второй. Единственный инструмент, которым этот прогон
рвёт связь, — остановка прокси. Измерено 2026-08-13, тремя пробами по коду деска
(`electron/services/trading-command-outcome.js`,
`futures-trading-adapter.js:90`):

| Что сделать с прокси | Что получает деск | Как классифицируется |
|---|---|---|
| Остановлен (никто не слушает) | `connect ECONNREFUSED`, 59 мс, `code: null` | **определённый** отказ |
| Заморожен до поднятия туннеля | `Proxy connection timed out`, 30,1 с, `code: null` | **определённый** отказ |
| Заморожен уже при поднятом туннеле | `ETIMEDOUT`, 20,2 с | **неоднозначный** — то, что нужно |

То есть неоднозначный исход даёт **только** третья строка: прокси должен замереть
в промежутке между «запрос ушёл» и «ответ пришёл». Туннель поднимается заново на
каждый запрос (keep-alive у SOCKS-агента выключен), и этот промежуток равен
одному кругу до Binance — **340–800 мс**. Попасть в него рукой нельзя.

Подделывать его на живом деске — держать переключатель, имитирующий отказы биржи,
— риск больше проверяемого.

Чем закрыто вместо этого: `binance-connection.test.js` и
`trading-command-outcome.test.js` — 503 и таймаут на размещении дают `UNKNOWN`;
бизнес-отказ биржи остаётся определённым; неоднозначное размещение, чей ордер
существует на бирже, никогда не порождает вторую отправку; предупреждение
снимается только ответом с тем же идентификатором, а не любым чужим обновлением.

## Блокировка расчёта размера между переподключением транспорта и первым ответом счёта

`say-which-readings-are-stale` 1.4. Это обрыв **внутреннего** сокета деска, не
биржи. Перезагрузка окна даёт новый сокет, но и новое состояние — наблюдать
нечего.

---

# Наблюдения оператора, требующие отдельной работы

- **Спот заметно медленнее фьючерса.** 2026-08-15, дословно: «работает всё
  гораздо медленней чем на фьючах. Я подозреваю что на споте, в отличии от фьючей
  — у нас много очень REST запросов, которые можно было бы так же оптимизировать
  перенеся некоторый функционал на WS». Сама проверка (шаг 13) прошла: история
  догружается, ничего не съезжает, `1M` открывается. Гипотеза про REST против WS
  **не проверена** — её надо мерить так же, как мерили фьючерсную книгу, прежде
  чем заводить изменение.
- **Total ордера в USDT при частичном исполнении.** 2026-08-15: оператор просит
  отдельно посмотреть, как обновляется общая сумма ордера, когда крупный ордер
  откупается по частям. Шаг 41 проверяет размер на четырёх поверхностях, но не
  поведение суммы во время самого исполнения.

# Что проверять пока рано

Не пытайтесь — работа не сделана:

- `prove-the-private-stream-is-carrying` — §1 закрыт кодом и проверен шагом 35.
  Остальное в работе: пока «сокет открыт» по-прежнему считается за «поток
  несёт», так что мёртвый маршрут снова читался бы как здоровый — шаг 35 ловит
  это только на живой проверке, а не сам по себе.
- `keep-the-history-read-out-of-the-way`, `make-the-order-history-readable` —
  запланированы, не сделаны. Шаг 30 п.10 собирает для второго «как было».
- `stop-reading-what-the-desk-can-count`,
  `compute-the-unstated-values-beside-the-read`,
  `time-the-frame-from-exchange-to-screen`, `report-execution-state-truthfully` —
  не начаты или в работе. (`keep-the-contracts-warm` сделан и проверяется
  шагом 39.)
- `keep-the-chart-loadable` дальше шагов 19 и 21 — потолок серии и пересчёт свечи
  в середине не исправлены.
- Тихий график всё ещё уходит из `live` за ~10 секунд: порог свежести свечей
  плоский (`CANDLES_MS: 5_000`), и отдельного изменения под него пока нет. Шаг 11
  проверяет, что это больше не запрет, а не что порог исправлен.
- Формулировка «Select an active USDⓈ-M contract» на `TRADIFI_PERPETUAL`
  (TBTUSDT, PYPLUSDT, GSUSDT) вводит в заблуждение: деск такие контракты не
  торгует намеренно. Отдельное изменение под это не заведено — скажите, заводить
  ли.
