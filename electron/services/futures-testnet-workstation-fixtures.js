import { createFuturesWorkstationFixture } from './futures-workstation-fixture-builder.js';

export const FUTURES_TESTNET_WORKSTATION_FIXTURE = createFuturesWorkstationFixture({
    timeOffset: 0,
    priceOffsetCents: 0n,
});
